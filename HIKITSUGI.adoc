= 引き継ぎ(2026-08-03)

別の環境で作業を引き継ぐ人(または Fable)への申し送り。
*設計と決定事項の正本は link:SEKKEI.adoc[SEKKEI.adoc]* — まずそちらを読むこと。
ここには SEKKEI に書かない「進め方の作法」と「直近の状態」だけを書く。

== 進め方の作法(発注者と確立済み)

* **`git add -A` を使わない。触ったファイルを名指しで add する。**同じ置き場で別の作業が同時に走っていることがあり、`-A` はその**作業途中を自分のコミットに巻き込む**(2026-08-09 に実際にやった。別の作業がマークダウンのセル描画と Python の作りを直している最中だった。`git reset --soft HEAD~1` → `git reset` で戻した)
* 発注者は実機で GUI を触り、日本語で不具合を報告する。*1報告 = 原因究明 → 修正 → `cargo test --workspace` 緑 → clippy 緑 → `cargo build --release` → コミット*、の回転。発注者は``./target/release/{writer,calc}`` を直接起動して確かめるので、*release の組み直しを忘れると「直っていない」と見える*
* **コミット前に clippy を回す。`cargo test` が通っても CI は落ちる。**CI は `--all-targets` で回すので、*試験の中の未使用 import* のような``cargo test`` が見ない物で赤になる(2026-08-10、条件付き書式の直しで試験に `CondLook` の未使用 import を残し、CI を全部赤にした。別のセッションに拾わせてしまった)。手元で走らせる形はこれ:

      cargo clippy -p sheet -p kumihan -p ooxml -p lang -p paper -p sidecar \\ --all-targets -- -D warnings -A clippy::too\_many\_arguments \\ -A clippy::type\_complexity -A clippy::field\_reassign\_with\_default \\ -A clippy::needless\_range\_loop *この並びに calc・writer・ui は入っていない。* GUI の3つは門番の外にあり、実際に `ui/src/pyedit.rs` の未使用 import などが溜まっている。

* *CI が走らせる試験も同じ6つ足らずしかない*(2026-08-10 に数えた):

      cargo test --no-fail-fast -p sheet -p kumihan -p ooxml -p lang -p paper つまり *calc の 137 本・writer・ui の試験は CI で一度も走らない。*「ready の嘘は wiring\_tests が落とす」という方針が拠っている当の``wiring_tests`` が、手元でしか走っていない。おそらく GPUI の連結に系のライブラリが要る(`.linklibs` の細工)のが理由で、*意図した除外かもしれない* — だが結果として、押しても何も起きないボタンを CI は止めない。`.github/` は別の持ち場なので、直すなら声を掛けてから。`lang/tests/i18n_soroi.rs`(文言の門番)は lang なので**走っている** 「検査はあるが、検査が見ている場所が本番とずれている」— 同じ日に4度踏んだ型(キーの嘘・文言の門番・clippy の範囲・試験の範囲)

* 原因は推測で直さず、`vendor/zed/crates/gpui` のソースで裏を取る
* **コミットは英語**で「何を・なぜ」(2026-08-10 発注者。GitHub で最初に読まれるため)。*設計・注釈・試験名は日本語のまま* — 中の作業の言語で、外の人が最初に読む物ではない。2026-08-09 までの履歴は日本語だが揃えない。設計判断と踏んだ穴は SEKKEI.adoc(部屋割りの先)に追記
* 方針: できないものを、できるように見せない(未実装は灰色。ready の嘘はwiring\_tests が落とす)/ 黙って落とさない(Report へ)/書式は据え置き / どの操作も1手で戻せる

== 環境の注意

* cargo が PATH に無い機械なら `export PATH="$HOME/.cargo/bin:$PATH"`。そもそも rustup ごと無い機械もある — その場合は user レベルの導入で足りる(`curl https://sh.rustup.rs | sh -s -- -y`。sudo 不要。2026-08-03 に実施済み)
* `.cargo/config.toml` と `.linklibs/` は**この機械固有**(libxkbcommon-x11 の開発リンク回避)。別の機械では不要か、作り直しが要る。作り直しは2手: `.linklibs/libxkbcommon-x11.so` → 実行時の `.so.0` へ symlink、`.cargo/config.toml` に `rustflags = ["-L", "<絶対パス>/.linklibs"]`
* 実物の様式(検査の材料)は``/mnt/sdb/home/dev/ドキュメント/機構/yoryou-yoshiki/`` にある。*無い環境ではテストは黙って飛ぶ*(失敗はしない)。可能ならコピーして持つ
* Python 検証は miniforge の `.venv`(polars / python-docx / docxtpl)。無ければ``conda create -p .venv python polars python-docx`` + `pip install docxtpl `(docxtpl は conda-forge に無いことがあるので pip で)

== 画面まわりの直しは実機で見る(2026-08-08 発注者指摘)

*「テストが通った」で報告しない。* 一覧の位置を直したと報告して「きちんとやれ」と言われた。
実機で動かしたら位置は直っていたが、位置の直書きが別に6箇所あり、Esc の短絡もあり、`deferred` で試した手は一覧を全部消していた。
*どれも机上では出ない。*

    cargo build --release -p calc python3 tools/ribbon\_sweep.py           # リボンの全ボタンを押して回るpython3 tools/kill\_sweep.py             # 点検で立てた分だけ落とす

*`pkill -f release/calc` を使わないこと。* 発注者が開いている窓まで巻き添えにする(2026-08-09 まで実際にやっていた)。
点検の道具は立てたpid を控えているので、後始末は `tools/kill_sweep.py` で。

一巡は「落ちない/押して何か起きる/一覧がボタンの真下に出る/Esc で閉じる」の4点を、画素比べでなく `calc.sock` の rpc(`ribbon`・`ui_state`)で見る。
しくじった時だけ撮る。
*しくじりが出たら、まず道具の側を疑うこと* —最初の版は 48 件報告して大半が道具の欠陥だった(詳しくはlink:docs/guide-tsukiawase-2.ja.adoc[台帳の第4便]の「誤報を生む型」)。

一巡で見つけた欠陥の**型**は画面なしの試験にも落としてある(`一覧が開くボタンは押した所に一覧を出す`)。
位置を直書きすると``cargo test`` で落ちる。

手で見るときの起動(この機械は GNOME Wayland。
XWayland 経由で X11 に出す):

    env -u WAYLAND\_DISPLAY XDG\_RUNTIME\_DIR=$(mktemp -d) DISPLAY=:2 ./target/release/calc &

**実行時ディレクトリを分けないと、動いている本人の calc に相乗りする。
**撮るのは `import -window <id>`(窓は `_NET_WM_PID` で引く。
ID も位置も起動ごとに変わる)。
Xephyr は DRI3 が無く GPUI が真っ黒。
GNOME のスクリーンショット D-Bus は拒否される。

== GPUI の踏み跡(再発させない)

* div の既定レイアウトは *Block(縦積み)*。入力とマウスの受け皿(InputSink)は absolute + inset 0 で全面に重ねる
* マウスの bubble 配送は**後に登録した方が先**。重ねるメニュー類はInputSink より後に描き、項目側の on\_mouse\_down で `cx.stop_propagation()`
* mouse-up は**ポインタが乗っている要素にしか来ない**。ドラッグ終了は``window.on_mouse_event`` + move 時の `pressed_button` 確認で自癒させる
* *click\_count で「押す系ジェスチャ」を分岐してはいけない*。Wayland は400ms 以内・近距離の押し直しを 2,3,… と数える(位置は押下同士の比較)ので、やり直しのドラッグがダブルクリックに化ける。calc の列幅境界の「ダブルクリックで自動調整」はこれで発注者のドラッグを横取りしていて撤去した(2026-08-03。入れ子 mutter + xdotool の自動再現で特定。測る仕組みはgit 12e71bf にある — 復活させるならメニュー等の押下と衝突しない場所へ)
* 文字の行の高さは既定で黄金比 → グリフは div の頭から約 0.309×サイズ下に描かれる(writer の `HALF_LEADING`)。自前で引く線や下地はこれを織り込む
* rfd のダイアログは同期 = メインスレッドを塞ぐ。終了確認・ファイル選択は``background_executor().spawn`` + `cx.spawn` で別のスレッドに出す。*calc・writer とも全ダイアログ(開く・保存・PDF・画像)を別スレッドに出し済み*(2026-08-03)。新しいダイアログを足すときも同じ作法で
* この作業木で **git sparse-checkout が突然有効になっていた**事故があった(2026-08-04。engine/ が作業木から消えた。`git sparse-checkout disable` で復旧)。誰の操作か特定できず — 見かけたら同じ復旧を。stash/checkout 同様、共有作業木では sparse-checkout も使わないこと
* *vendor/web-apps は元の機械の版でないと再生成できない*(2026-08-04 にONLYOFFICE master を取ってみたらテンプレート構成が変わっていてタブが欠けた。この機械では ribbon.rs への手反映+gen\_ribbon.py の同期、の二重書きで運用)
* リボンは生成物: `python3 ui/gen_ribbon.py ja > ui/src/ribbon.rs`。押せるものは同スクリプトの READY 表。テンプレートの class 注入スロット(pagebreak・insertimage 等)は id の正規表現に掛からないので手で差し込む

== 実装の注意

* 保存は `write_with(原本)` が基本(xlsx / docx とも)。原本を渡さないと図形・スタイル等の部品が消える
* *engine の共有の型に手を入れたら ``cargo check --workspace --all-targets``を1回。20秒で済む。* `kumihan::Paragraph` や `InlineImage` に性質を足す・`ParaStyle` に枝を足すと、全指定 literal と網羅 match が**離れた crate**で割れる(engine/examples/page.rs、ooxml と paper の試験、pysheet)。 2026-08-17 に実際に踏んだ: 2026-08-16 の `InlineImage.src` と``ParaStyle::Quote`` で *pysheet と paper が 15 commit のあいだ組めなくなっていた*。手元では触った crate だけを試験していたので気付かず、CI は赤いまま誰も見ていなかった。*「全部の試験は回さない」は正しいが、workspace の check は試験ではない* — 別物として毎回やる。編集の持ち越しは *run 粒度の splice*(SEKKEI「writer の編集モデル」)—段落の性質も run の境も、編集で流されない
* calc の編集判定は `editing()`(数式バーとセルの保存内容の差)。「バーが空か」で分岐してはいけない(バーには常にセルの中身が写る)

== 次の仕事: 本家ガイドとの突き合わせ点検(発注者確定 2026-08-06)

*ONLYOFFICE の公式利用者ガイド(日本語)を上から辿り、calc の現状と突き合わせ、画面や挙動の食い違いの一覧を作って修正する。*

* 入口: https://helpcenter.onlyoffice.com/ja/docs/userguides/spreadsheet\_editor (節ごとの個別ページを WebFetch で読める。目次: インターフェース各タブ /基本操作 / 文字の書式 / セルのプロパティ / 行・列(並べ替え・フィルター・条件付き書式・ピボット)/ 数式・関数 / オブジェクト / シート操作 / 保護 /ツール / コラボレーション / ショートカット)
* 進め方: 節を1つ読む → calc の現物(コードと release 起動)と突き合わせ →食い違いを一覧に控える(SEKKEI に節を作る)→ **1件=原因究明→修正→ cargo test --workspace 緑 → release 組み直し → コミット**の回転。見た目だけで判断が割れるものは発注者の実機確認に回す
* 2026-08-06 までに本家合わせで直した実績(やり方の手本):数式バーの三点構成(名前ボックス・fx・関数の引数の第2段)、ホームタブの「組ごとの上下の対」(単純な2行割りではない — HOME\_PAIRS)、終了確認を窓の中のパネルに、格子は窓で伸縮させない、ほか SEKKEI「calc の5つの直し」以降の節を参照
* 注意: 共有作業樹に別セッションが並走することがある — コミットは**自分の触ったファイルだけ名指しで add**。ribbon.rs を手で直したら各言語の追随は gen\_lang.py(en だけ gen\_ribbon\_locale.py)
* *第1便 済(2026-08-06)*: 一覧は SEKKEI「本家ガイドとの突き合わせ点検」。直した: リボンの取りこぼし(挿入のピボット/コメント/ヘッダー・フッター、ホーム/データの昇順・降順、レイアウトの整列系(灰)、表示の標準/改ページプレビュー(灰)、描画の選択)、保護タブを SSE の形に、*シートの管理*(タブの右クリック: 挿入/削除/名前の変更/コピー/左右移動/非表示/再表示/タブの色。改名は式のシート参照も追随。tabColor は xlsx 往復)、文言の門番 gen\_i18n を緑に(未訳42句を13言語へ)。残りの節: オブジェクト各論・関数を使う・ツールと設定・ショートカット。控え(未修正)は SEKKEI の一覧の 8・9・11〜14 — 大きい順に検索と置換の選択肢、書式のコピー(刷毛)、オートフィルの右ドラッグ
* *第2便(2026-08-06)*: 残り約50節をワークフローで全量調査 —*確定97件、台帳は docs/guide-tsukiawase-2.ja.adoc*(✔で消し込む)。消し込みの順番は SEKKEI「第2便」節。ここまでに直した: 1段の6件(F9/Shift+F9 — *手動計算が丸ごと効いていなかったのも同根で修正*、Alt+Enter 改行+折り返し、結合で中央揃え、大小5択、固定のプリセット、消去にコメント/リンク)と、2段の大物3つ —*オートフィルタ第1版*(見出しの▼・値のチェックボックス・検索・即時反映・行番号青・状態行に n/m 行。持ち方は「隠す値」の集合。条件/色/トップ10と xlsx 保存は第2版へ)、*並べ替えの複数基準*(聞き取りのパネル「金額 降順, 品名」。sheet::sort\_by\_columns)、*数値形式の一覧+コード直打ち*(format\_value に指数と @ を追加)。*データ検証の拡充も済*(整数/小数/文字数+全比較+入力メッセージ/エラーの文言。全種類を xlsx と往復 — list 以外を落とす仕様は廃止。判定できない規則は堰き止めない)。*2段=表の仕事の本丸は完了*。*次の段(3段=帳票の見た目)*: 罫線の線種・太さ・色(sheet のBorders が bool×4 なのでモデル拡張から)+12種テンプレート、色のその他(RGB 直指定)、文字の角度の任意指定。その後4段: ヘッダー/フッター(灰色ボタンの実体化)、名前マネージャー、条件付き書式のルール種、画像の選択・移動、詳細設定
* 作法の覚え: 文言を足したら gen\_i18n --missing → en に追記 → 12言語の材料は「HEAD の keys.json と新材料の2ポインタ整列」で振り直す(このセッションで4回実施。台本の形は git log の i18n コミット参照)

== 2026-08-19 の状態(Fable → Opus への引き継ぎ)

*この日の 27 コミットで「エンジンの統一」1〜4段目と「画面を1つにする」1〜5段目が入った。*
*残りの実施方針は link:SEKKEI.adoc[SEKKEI.adoc]「残りの実施方針」に A〜E で書いてある。* まずそこを読むこと。
*A-4 以降の統合は同じく SEKKEI「writer と calc を officework に統合する」(2026-08-19 設計)が正本。* タブ1つ=ファイル1つ=編集画面1つ、が核心。

*残りは段7の後半・段8・段11。見た目の決めは全部出ている(2026-08-19 深夜)*:
一覧は右で1本化・**検索は左**(発注者「検索は語句の検索のためのものなので別の操作」— 右=物を選ぶ・左=探す/対話)/
ファイルのページは officework の1枚(いま見ているファイルの操作だけタブに応じて切り替え)/
上の2行は**タブの行だけ**移すと決まり、`ui::tabrow` で**済**(writer 142行+calc 201行の写しを畳んだ。描く位置は隣り合いの都合で編集画面の中のまま — 持ち主と実装が1本になった。段8の入口の付け替えはこの部品の上で)。

*統合の設計は 2026-08-19 の夜に閉じた。決めは3つ全部出ている*:
配布は officework 1本 / 統合できる物はできるだけ統合(外枠は全部 officework。二重の外枠 13 件の在庫と統合先は詳細設計の表)/ 開き直したら前回の姿に戻す(フォルダ・ファイル・見ていたタブ。`session.txt`)。
**統合は段1〜5・段9・段10 と、段6・段7 のコードの分が済んだ(実施方針の A-4 を含む)。**

**残っているのは見た目の決めが要る3つと配布**(段6 の後半・段7 の後半・段8・段11)。
*3つとも「どこに何を置くか」の決めが先*で、コードはその後。
「題の行は残す」と決めたときと同じ性質の問いなので、まとめて見てもらうのが早い。

*実機で触るときの見どころ*(この回で挙動が変わった所):

. **タブ** — 文書と表を混ぜて何枚でも開ける。書きかけには `*`、× で閉じる(書きかけは断る)
. **持ち替えで消えない** — 表を打ちかけたまま文書へ行き来できる
. **閉じるとき** — 書きかけのタブがあると閉じない。どれかを名前で言う
. **開き直し** — 前回のフォルダ・ファイル・見ていたタブが戻る
. **段の選び** — ホームを見たまま表へ行ってもホーム
. **表の画面のキー** — Alt+Enter・Alt+S・Alt+C・Ctrl+1 などが効くようになった
. **バージョン履歴** — 控えの名前に日時が入る(macOS と Windows では入っていなかった)
11段それぞれに確かめ方が書いてある。

段1〜5 で入った物:

* *タブは officework の持ち物*。文書も表も1本の並びに出る。書きかけには `*`、× で閉じる(書きかけは断る)。**持ち替えで作り直さないので書きかけが消えない**
* *閉じるときは全部のタブに聞く*。`on_window_should_close` は officework の窓に**付いていなかった**(writer と calc が自分の `main` で結んでいた)ので、確認なしで閉じていた
* *「開く」の窓は officework が出す*。前は文章の画面から表が選べなかった
* *開き直すと前回の姿に戻る*(`face::session`。`settings.toml` の `folder` は退役)
* *割り当ては `jo_doc` / `jo_sheet` の2文脈*。**calc 専用の 31 鍵が officework で1つも効いていなかった**のと、`Ctrl+E` / `Ctrl+R` の意味の食い違いが直った
* *段の選びが画面をまたいで残る*(段6 の前半)。段の番号は画面ごとに違うので、`ui::tabs::merged` の位置に直して持ち越す
* *受け口に宛先(`path`)*。裏のタブにも届く。**見ているタブは動かさない**

*段6 の後半(上の2行の引っ越し)を残した理由*: 測ると writer 約142行・calc 約201行で、
実装済みの数え・文脈タブ・保護の状態・検索・明暗と編集画面の中身に深く結びついている。
**段の選びが残る**という眼目は前半で得られるので、描画の引っ越しは別の回にした。
着手するなら、両方の行の組み立てを1つに抜き出してから移すのが安い(そのまま2つ写すと、
同じ物を2箇所に持つことになる)。

*測った物*: タブ1枚あたりの重さは数 MB(文書1枚 207.7MB → 表を足して 212.3MB →
もう1枚で 212.5MB)。大半は GPUI と書体で共通。小さいブックでの値なので、
大きいブックを何枚も開く使い方が出たら測り直すこと。
*段の一覧は詳細設計に1つだけ*(前は7段と9段の2つがあり、番号の指す物が食い違っていた。2026-08-19 に一本化)。

このほか夜の見直しで: 「耳」「泵」を全部言い換えた(タブ・シート見出し。kotoba_check にも足した)/ 暗号化したブックを `.adoc` で保存すると暗号が黙って外れる穴を塞いだ / writer の clippy を 0 にした(calc / writer は CI の clippy の外にあるので、コミット前に手で回すこと)。

=== 動くようになった物(実機で見た物だけ)

* *`officework` が1つのアプリとして動く。* フォルダのファイルを選ぶと、名前で文章の画面と表の画面が切り替わる(`.sheet.adoc` は表、`.adoc` は文章)。編集画面は writer と calc のままで、1つの型にはまとめていない
* *`.adoc` がブックの正本になった。* calc が開いて保存する。実物 16 冊で値の往復を確かめてある(`sheet/tests/adoc_round.rs`)
* *1つのファイルに文書を何枚も*(同時に送る請求書の原稿)。切れ目は `[discrete]` を付けた `= 題`。下のタブで行き来する
* *ファイルのタブ*(Zed と同じ形)。右パネルにフォルダの一覧
* *文書の表でセル関数*(`ops::table`)。計算は calc と同じ道を通す
* *受け口は3つ* — `calc.sock` `writer.sock` `officework.sock`。ソケットの世話は `ops::listen` に1本

=== 引き継ぐ人が先に知っておくと良い事

* *`face/src/ribbon.rs` は生成物*(「手で書かない」と先頭に明記)。対応表をそこに書くと次の生成で消える。段の突き合わせは `face/src/tabs.rs`(手で書く場所)に置いてあり、*対応は書かず走るたびに表から作る*
* *生成器と `ribbon.rs` は既にずれている。* 再生成すると段の数が変わる(Python・プラグイン・AI の段が手で足されている)。**B 群はこのずれを解くのが先行条件**
* *`cargo test -p face tabs:: -- --ignored` は単独で回す。* 言語の初期化を先に触るので、まとめて走らせると `settings` の言語の試験を落とす。言語がいつでも変えられるようになったので、この縛りは根拠を失っている(E-2 で外せるはず)
* *calc の `ui_state` に `right_open` / `right_face` / `left_open` を足した。* これが無い間、実機の点検で当て推量に押して検索の窓とセル編集を開いた。**calc の画面を触るときは rpc で状態を見てから押す**
* *writer は `OFFICEWORK_UI_DUMP` に状態を書く*(`tools/writer_shot.py`)。`officework` は中身が writer なので、実行ファイルを差し替えれば同じ道具で駆動できる

=== 確かめていない事(そのまま引き継ぐ)

*この3つは 2026-08-19 に片づいた*(下の「2026-08-19 の続き」)。残りは A-1・A-4・A-6。

* [.line-through]#表 → 文章の持ち替えを画面で見ていない#(A-5)→ *確かめた*。rpc で駆動して撮った
* [.line-through]#`.sheet.adoc` で保存する窓が無い#(A-2)→ *直した*(70379ce)
* [.line-through]#持ち替えで書きかけが黙って消える#(A-3)→ *直した*(b1c3a4a)

=== 2026-08-19 の続き(Opus)

* *A-2 済*(70379ce)。`face::folder::as_sheet_adoc` を足した — `kind_of` の逆向きで、保存の名前を `名前.sheet.adoc` に揃える。**種類を決めるのが1箇所なら、種類に合う名前を作るのも1箇所**。試験は「名前の作り方」と「作った名前を `kind_of` が表と読むこと」の2本を対で置いた
* *A-3 済*(b1c3a4a)。画面から押した持ち替えと受け口の `open` の*両方*で断る。writer は*開いている全部のタブ*を見る(持ち替えは画面ごと作り直すので裏のタブも消える)。受け口から断ったときも状態行に出す
* *A-5 済*。上の A-3 の実機確認の最後が表 → 文章の持ち替えで、そこで一緒に見えた
* *E-1 済*(f949093)。**「2件」ではなく3件あった** — 3件目は `ooxml/examples/hakaru.rs`。例は `--all-targets` にだけ出るので数え落としやすい

* *A-1 済*(f54401b)。引数 → 前回のフォルダ → 選んでもらう、の順。`Writer::show_folder` はファイルを開かず一覧を出すだけ。開いたファイルのフォルダを覚えるので「前回のフォルダ」が自然に育つ
* *文言の門番の穴を塞いだ*(同じ回)。`ui/gen_i18n.py` と `lang/tests/i18n_soroi.rs` が `calc/src` `writer/src` `ui/src` の3つしか見ておらず、**後からできた `officework` と `face` が目の外**だった。face の札(フォルダ・文書・見た目・様式)が 13 言語で日本語のまま出ていた。「部屋が増えたら足すのではなく glob で」と書いてあったのに、*クレートが増えたときに同じ穴が開いていた*

*次に着手するなら A-4*(ファイルのタブを officework の持ち物に)。その次が A-6(配布)。

=== まだ確かめていない事(Opus から次の人へ)

* *フォルダを選ぶ画面が出るところを見ていない。* 覚えたフォルダが無いときに `rfd` の `pick_folder` を背の糸で呼ぶが、この機械では portal 越しの窓が出なかった(D-Bus の宛先が届いていないと見ている)。**窓が出ない道でも編集画面はふつうに出て使える**ことは確かめた。実機で見たらこの行を消すこと
* 上の裏返しで、*選んだフォルダが一覧に出るところ*も見ていない。`show_folder` 自体は「前回のフォルダ」の道で見えている(同じ関数を通る)

*踏んだ穴*: `ui/gen_lang.py <loc>` は**ファイルを自分で書く**。`> lang/src/i18n_<loc>.rs` と繋いだら、生成物に画面の報せが流し込まれて 13 言語が UTF-8 として壊れた。`gen_ribbon.py` は標準出力に出す形なので、*2つの生成スクリプトで作法が違う*。もう一度回せば直るが、**先頭の使い方を読むこと**。
なお `ui/i18n/*.json` の綴りは*1行ではなく `indent=1`* — 昔の申し送りが古い。`json.dumps(d, indent=1, ensure_ascii=False) + "\n"` で元と1バイトも変わらない。

=== この日の教訓(同じ穴を踏まないために)

* *「動くはず」で報告しない。* 表 → 文章は組んだが見ていないので「確かめていない」と書いた。分けて書くと、次の人が何を先にやるか分かる
* *測ってから決める。* 文書の切れ目は4つの書き方を試し、警告だけ見て採っていたら「文書の中に `--` があると2枚目が丸ごと消える」案を選ぶところだった。**中身まで測る**
* *実物で測る。* `.adoc` の往復は作り物では通り、実物 16 冊で 5 冊落ちた(頭に 0 の付いた伝票番号が数に化けた)
* *生成物に手で書かない。* リボンの対応表を生成物に書き、次の生成で消える形にした。**ファイルの先頭を読む**
* *試験の後片づけはファイルごとに分ける。* 4つの試験が同じ一時フォルダを消し合い、単独では通るのにまとめて走らせると落ちた
* *ライブラリに切り出すと試験が増える。* calc を lib にした途端、それまで一度も走っていなかった doc-test が動いて落ちた

== 直近の状態(詳細は git log)

済: calc の右クリックメニュー(Euro-Office 準拠・灰色ゼロ)、Paste Special(独自)、複数シート、条件付き書式・コメント・リンク・名前の定義(xlsx 往復つき)、コピー範囲の破線、列幅・行高のドラッグ(見出しの境界を掴む。
undo 可・xlsx 往復は元からある)、列・行の選択(見出しをクリック/撫でる/Shift+クリック。
左上の角で全選択。
見出しの右クリックは選んでからメニュー。
選択中の見出しは色が変わる)、グラフの挿入(裏方 matplotlib。
SEKKEI「calc の分業」の改訂を参照)と画像の挿入・表示(浮かぶ絵。
読んだ xlsx の絵も出す。
保存は writer と同じimages/images\_new の二本立て — 原本の drawing には**アンカーを継ぎ足す**)、検索と置換(パネル2枚。
空 Enter=次を検索)、書体と大きさの一覧・大文字小文字、関数の拡充(VLOOKUP/INDEX/MATCH・TODAY/DATE 系・PMT 系。
数式タブ灰色ゼロ)、印刷の仕上げ(拡大縮小・改ページ・タイトル行の繰り返し・枠線/見出しの印刷。
printOptions は今まで持ち越しから漏れていた — 直した)、図形の挿入(SVG 方式 — SEKKEI「calc の分業」参照。
六種、選択・移動・右下で大きさ変更・Del 削除。
xlsx へは prstGeom、PDF は輪郭のみ)、テキストボックス(図形+文字。
文字は GPUI の重ね描き=日本語の組みが正、xlsx へは txBody 往復。
図形を選んで Enter でいつでも書き直し)、スパークライン(選択の数を折れ線に。
**その時の値で描く固定の線**とstatus で言う。
xlsx へは custGeom の折れ線 = Excel でも見える)、排他ロック(.~lock.名前#。
ファイルサーバーの「後勝ちで潰す」を防ぐ。
先客が居れば名乗りを見せて上書き保存を止める。
*writer にも同じものが要る* — lock\_path\_for / foreign\_lock / lock\_identity は calc/src/main.rs にある。
共通化歓迎)、リボンの全部入れ(発注者確定: コラボレーション・保護・プラグインのタブを両アプリに追加。
場所は本家どおり・当面灰色。
calc のコメントを追加だけ配線済み。
writer 側の コメント/変更履歴 のボタンは相方の実装に繋げられるはず — 申し送り。
2026-08-04 第2弾で本家の残りタブも追加し**全タブ完了**: calc にピボットテーブル・表のデザイン(データの直後)、writer に フォーム(参考資料の直後)。
表記は本家 ja.json、位置指定は gen\_ribbon.py のAPP\_TABS。
表のデザインの「重複データを削除」だけ既存の rem-duplicates に配線、他は灰色。
タブの有無は試験「本家のタブが全部ある」で固定)、ピボットテーブルの挿入(発注者確定: polars 裏方で挿入だけ動かす。
パネル3枚で 行 → 列(省略可)→ 値と集計 を聞き、結果はその時の値で元の表の右の空きに置く。
新しいシートは undo で消せないのでシートには足さない。
台本は PIVOT\_PY、指図は JSON、答えは CSV 取り込みと同じ区切りの印。
合計の無い組み合わせは polars の流儀で 0 になる — 試験がそう言っている。
2026-08-04 続き: タブの残り全部 — 指図 PivotDef を xl/joPivot.xml で往復(joPython と同じ独自部品方式)、更新=同じ場所に polars で置き直し(広がって塞がる時は断る)、総計/小計/空行/レイアウトは指図の旗+置き直し。
台本の答えは行ごとに種別つき(h/d/s/b/t)で、小計・総計は太字になる。
総計の「1行に集める」は stub の小技=見出し列を札で潰してピボットに通すと列名が揃う。
試験は台本の実走+指図の往復。
ピボットタブ灰色ゼロ)、グループ化(データタブの4ボタン。
モデルは Sheet の row/col\_outline(深さ)とrow/col\_hidden(畳み)、xlsx の outlineLevel/hidden と往復 — 中身の無い行の<row> も捨てない。
cols は幅・深さ・畳みを1走査でまとめて出す。
画面はvisible\_rows/visible\_cols の一点で飛ばし(当たり判定も同じ道)、紙(PDF)も幅・高さゼロで飛ばす。
詳細の表示/非表示は選択、無ければカーソルの行のひとつながり。
アウトラインの +/-(gutter)も済(2026-08-04):かたまりが**終わった直後の行**の見出し左に丸ボタンを描き、− で畳み + で開く(見出しの重ね描き。
列の gutter は無し — 詳細の表示/非表示で足りる)。
発注者指摘「畳んだとき合計が残らないとだめ」→ **小計**のボタンを追加(Excel の データ > 小計。
本家のデータタブには無いが EXTRA\_CMDS で置く):区切りの見出し → 合計の見出し(空=数の列全部)を聞き、apply\_subtotals が区切りごとに =SUM の小計行+総計行を挿し、**明細だけ**グループ化する。
枠の挿入は下から(上の座標が狂わない)、式は最終座標で書く。
insert/remove\_row/col が row/col\_outline・hidden を運ぶよう直した(置き去りだと別の行が畳まれる)。
試験あり。
※入れ子 mutter の自動操作の罠: 打鍵が届かないときは X の入力フォーカスを明示的に当てる(python-xlib: set\_input\_focus。
xdotool 無しでもXTEST + xwd で回せる。
Shift+クリックの修飾も同根で落ちる))、calc の共同編集・プラグインの7ボタン(writer の仕組みの移植 2026-08-04:共同編集モード=錠の顔 / コメント削除・表示切替 / チャット=.chat.txt /バージョン履歴=.jo-history 9世代・一覧は pick 流用・開くと名無しの複製 /マクロ=run\_python\_file\_dialog と同実体 / プラグイン管理= ~/.config/officework/plugins の一覧実行。
保存前の keep\_version は save\_to に。
同日、保護タブ3ボタンも完了: 保護=xlsx の sheetProtection(シート単位。
堰は commit / fmt / 貼り付け / Del / run\_cmd の白表 PROTECTED\_OK)、暗号化=ooxml::crypt を保存/開くに結線(原本持ち越しは original\_plain で平文に解いてから)、署名=writer と同じ .sig / sign.key。
パスワードのパネルは伏せ字。
calc のコラボ・保護・プラグイン灰色ゼロ。
続けて**画面の組み立ても calc へ写した**: 1段目=QA+ブック名(緑の行・取っ手)、2段目=白地タブ+緑下線+🔍、下端=シートのタブ+状態+選択の合計/平均/個数(sel\_stats。
200,000 セル超は数えない)。
保護シートのタブに 🔒)、表のデザインの実装(表オブジェクトは持たない — 選択に1手ずつ掛ける道具:ヘッダー行の色・縞模様の行/列・最初/最後の列の太字・合計行=下に =SUM の行を足す(下に中身があれば断る)・フィルタのボタン=setfilter と同実体。
*方針変更 2026-08-04(発注者「表オブジェクトを持たない設計は方針変更」) → TableDef を持つことにした*: xl/tables/tableN.xml + tableParts + rels + Content\_Types と往復。
範囲に変換=表を外す(書式は残す)、サイズ変更=パネルで範囲を打ち替え。
表を減らしたら部品も CT の宣言も消す(残すと Excel が壊れたと言う)。
表のデザインの各ボタンは、表の中なら表の性質も一緒に更新。
同時に**右横書き**も実装(発注者「日本語は、たまに右から書く場合がある」)— Sheet.rtl ↔ sheetView rightToLeft は visible\_cols() を逆順にする一点だけ(描画も当たり判定も同じ関数を通るのでずれない)、CellFormat.rtl\_text ↔ readingOrder="2" はセルの中を1字ずつ右から。
ラテン文字の bidi は扱わない)、PY の UDF とスピル(=PY("関数",…) セル。
@計算 のときだけサンドボックスで評価、開く=実行なし。
2次元はスピルし衝突は #SPILL!。
前史は発注者の 2018 Qiita)、*Python in Calc*(データ > Python。
パネルに一行 / .py 実行 / @save でブックにコードを載せて持ち運ぶ。
載ったコードは**必ず bubblewrap のサンドボックス**で実行、開いても自動実行しない。
設計は SEKKEI 参照。
officework/\_sheet.so を calc の隣に)、データタブの Python 裏方ほか(テキストからデータ=CP932 も判定する CSV 読み、区切り位置、ゴールシーク=割線法を表の複製で回す、外部リンク=値で取り込む。
ソルバーも実装(発注者確定 2026-08-04: 入力画面は ONLYOFFICE の「ソルバーのパラメータ」と同じ形の小窓 — 目的/最大・最小・値/変数セル/制約の追加・変更・削除/非負/単体法 LP。
多欄の小窓は solver.focus で打鍵の宛先を切り替える(HasEditor が見る)。
係数は表の複製から測り(全0→単位ベクトル→全1で線形の検算)、線形でなければ正直に断る。
解くのは SOLVER\_PY = scipy.optimize.linprog(highs)。
scipy は .venv にpip で追加済み。
セル参照は小文字も $ も受ける)、SmartArt(発注者提案「ONLYOFFICE からデータを取得」: 分類・並び・名前はweb-apps の define.js + ja.json の現物、SMARTART 表に採録。
挿入は図形の集まり(9形: カード型/縦方向/ピラミッドのリスト・基本ステップ/プロセス/タイムライン・基本の循環/ボックス循環・組織図/階層・ベン図・マトリックス・ピラミッド)。
*SheetShape に dx\_px/dy\_px を新設*(アンカーセルからのずらし。
oneCellAnchor の colOff/rowOff へ書き、画面・PDF・当たり判定・ドラッグが織り込む — ドラッグは px 精度になった。
読みも対応済み(2026-08-04: colOff/rowOff を読んで dx/dy に入れる。
往復試験あり)、方程式(発注者「writer の仕組みを移せば」: writer の方式=図は Python で描いて画像で貼る、を calc 流に自動化 — パネルに TeX(mathtext)を打つとEQ\_PY が清書して透過 PNG をカーソルへ。
読めない式は canvas.draw で止まって status に出る。
試験は実 matplotlib で PNG の中身まで)、スライサー(発注者「簡単では」: カーソルの列の一意な値をボタンで並べる小窓。
押して絞る・≡=複数選択・✕=解除・Esc で閉じる。
絞りは visible\_rows のslicer\_keeps で、絞り込みと同じ**見え方だけ** — xlsx のスライサー部品は書かない。
値は64個まで表示)、チェックボックス(セルの部品: Bool のセルを ☑/☐ で描き(カーソル上は編集の真実 = TRUE/FALSE のまま)、*空白キーで切替*(replace\_text\_in\_rangeの入口で、打ちかけ・パネル・小窓が無いときだけ空白を奪う。
保護も堰く)。
挿入ボタンは空のセルに FALSE を置くだけ — 中身のあるセルは潰さない。
xlsx へは普通の TRUE/FALSE(cellControls の部品は書かない=正直な劣化)。
紙(PDF)も対応済み — ただし ☑/☐ の字は Noto CJK に**無い**(実測)ので文字ではなく線で描く(箱+チェックの折れ線)。
豆腐を刷らない)、テキストアート(方程式と同じ道 = insert\_py\_image に共通化。
TEXTART\_PY が太字+縁取り(calc の緑)の飾り文字を透過 PNG に描く。
*挿入タブ灰色ゼロ*)、calc のファイル選択を別スレッドへ(開く・保存・PDF。
保存が済んだときだけ終了)、PDF の塗り・文字色・条件付き書式(切れた列の数も返して status に出す)、データの入力規則(list を読み・効かせ・往復・リボンから作成/解除。
設計は SEKKEI「calc に残すもの」。
条件付き書式と名前の管理もリボンから)、PDF が帳票の印刷設定に従う(pageSetup の向き・用紙(B は JIS)・pageMargins・Print\_Area。
効かせたものは status に言う)、レイアウトタブの印刷設定(向き・用紙・余白・印刷範囲を設定できる。
Print\_Area はモデルに昇格し undo 可。
保存は原文の pageSetup に**属性だけ差し替え** — 拡大縮小など知らない属性は残す)。
writer の選択描画・ドラッグ選択・全カーソル操作・右クリックメニュー・段落の背景色と囲み枠・画像の挿入(media/rels/Content\_Types 生成)・ヘッダー・フッター(パネルで編集・ページ番号は PAGE フィールド往復・PDF は各頁の番号。
設計は SEKKEI「writer のヘッダー・フッター」)・見出しスタイルと目次(ホーム > 段落のスタイル → 参考資料 > 目次。
設計は SEKKEI「writer の見出しと目次」)・全ダイアログを別スレッドへ・複数レベルのリスト(印はレベルで変わる。
Tab/Shift+Tab で深さ)・ページ数と日付(ヘッダー/フッタータブ灰色ゼロ)・ダークモード(紙は白のまま。
表示タブ灰色ゼロ)・行番号・ファイルからのテキスト・テキストの追加・目次の番号の右揃え(字幅で点線を詰める)・キャレットの大きさ追従・しおりとコメントのアンカーの保存持ち越し(黙って捨てていた穴を塞いだ)・挿入系の配線(図形・グラフ等=画像を貼る/テキストボックス=1×1の表。
SVG も受ける — resvg で高精細の PNG に直す。
matplotlib は savefig("図.svg"))・コメント(段落単位。
comments.xml 往復)・ページ色(w:background + displayBackgroundShape)・透かし(ヘッダーの VML と往復。
紙は回転した字)・ブックマーク(名前の往復+一覧・移動・追加・削除のパネル)・図表番号(「図 N」の中央揃え段落)・図表目次(Tof の印。
目次と同じ差し替え)・段組み(w:cols 往復。
細い行長で組んだ巻物を fold\_columns がページの物理座標へ折る — 座標を使う側は無変更。
発注者の指摘どおり難しくなかった)・描画(ペン・蛍光ペン・消しゴム。
設計は SEKKEI「writer の描画」)・変更履歴(保存時に差分を w:ins/w:del へ。
設計は SEKKEI「writer の変更履歴」)・ドロップキャップ(Word の作法で framePr の枠の段落に割って書き、読みで合流)・ハイフネーション(Knuth-Liang パターン同梱。
settings の autoHyphenation 往復)・*編集モデルの run 粒度化*(部分書式=選択の字だけ太字などが可能に。
段落の増減で性質がずれる持病・表が末尾へ動く持病も解消。
設計は SEKKEI「writer の編集モデル」)・相互参照(run が持つ REF/PAGEREF。
設計は SEKKEI「writer の相互参照」)・writer の排他ロック(calc と同じ .~lock.名前#。
先客が居れば上書きせず別名保存へ。
終了確認も calc と同じく実ファイルの未保存変更だけ)・*コラボレーション・保護・プラグインの8ボタン*(発注者確定 2026-08-04「灰色のままにしない」: 保護=documentProtection readOnly の往復+効き目本物 / 共同編集モード=排他ロックの顔 / バージョン履歴=

.jo-history に9世代の控え+パネル / チャット=隣の .chat.txt の言伝 /
マクロ=サンドボックスの中の python-docx が複製を直す(Ctrl+Z で戻る)/プラグインの管理=~/.config/officework/plugins の一覧実行 /暗号化=ECMA-376 Standard AES-128 を ooxml::crypt に自作(msoffcrypto で解ける=形式が本物。
Agile はまだ読めないと言う)/署名=隣の .sig に Ed25519 の添え書き。
設計は SEKKEI の「writer の保護と共同編集」)。
writer は 88/101(残る灰色はフォームタブ10・縦書き・sdt・配色)。
*calc の同じボタンはまだ灰色* — ooxml::crypt(暗号化)や writer のlock の顔・.chat.txt・.jo-history の作りはそのまま移せる(申し送り)。
calc の表示タブを本家に揃えた(発注者スクショ 2026-08-04: ズーム・数式バー・見出し・0を表示する を追加。
ズームは col\_px/row\_px/セルの文字をself.zoom 倍、行高ドラッグは /zoom で割り戻す。
見出しは head\_w()/head\_h()に集約し 0 にできる — 当たり判定・原点・グリップも同じ関数を通すので消しても掴めない事故が無い。
0 表示は描画で空文字に差し替え=値は不変。
シートの表示・インターフェイステーマは灰色。
※実機の押下確認は入れ子mutter の装飾ぶれで座標が滑り不安定 — タブ描画の目視と wiring\_test で担保)。
*残りの灰色を全部つぶした*(発注者「残りを作ってしまいましょう」2026-08-04): 下付き(vertAlign)・両端揃え(HAlign::Justify を新設)・文字の向き(CellFormat.rotation。
画面は縦積みで見せる — GPUI に字の回転が無いため。
xlsx へは textRotation で往復)・計算方法(auto\_calc。
手動時は「計算」で回す)・関数の挿入(分類 → 既存の関数一覧へ)・セルのスタイル(CELL\_STYLES = 名前と書式の組。
表オブジェクトは持たない方針どおり普通の書式を掛けるだけ)・シートの表示(Sheet.hidden を新設。
workbook.xml のstate="hidden" と往復。
原文へは patch\_sheet\_states で属性差し替え)・ウォッチウィンドウ(watch: Vec<(シート, セル)>。
値は下のステータスバーに生きて出る)・描画3種(writer と同じ tool の入切)。
*筆も実装済み*(2026-08-04): 1筆をSheetShape の折れ線(kind="ink" / "marker")として置く — 既にある図形の仕組みに丸ごと乗るので custGeom で xlsx に入り(Excel でも線)、PDF にも出て、Ctrl+Z も効く。
描きかけは点の粒で見せ、離すと1本の線になる。
消しゴムはink\_at の近傍判定で1筆消す。
※罠: 移動の受け口(on\_mouse\_event のMouseMoveEvent)は drag 系が立っている時だけ mouse\_drag\_at を呼んでいた —筆と消しゴムの条件を足さないと「道具は出ているのに描けない」になる。
*calc 139/145*。
残る6つは方針で灰色維持(表オブジェクト2・RTL2・テーマ色2 = a:theme の読みが要る。
writer と同根)。
*テーマ色*(発注者「テーマ色の読みもやりましょう」2026-08-04): sheet/src/theme.rs を新設 — xl/theme/theme1.xml の clrScheme を読み(theme1.xml の並びは dk1,lt1,… だが `theme=` の番号は 0=lt1 なので入れ替える)、`<color theme="4" tint="0.4"/>` を HSL の明度加減で実際の色に解く。
**CellFormat に由来(color\_theme / fill\_theme)も持つ**ので、配色の変更で組を入れ替えるとセルの色が追従する(書き出しも theme= のまま返す)。
配色は SCHEMES(Office / 暖色 / 寒色 / 墨)。
テーマ部品は持ち越さずモデルから書き直す(でないと配色の変更が効かない)。
インターフェイステーマ=画面の明暗も実装 — *セルは白のまま*、暗くするのはリボン・タブ・ボタン・見出しだけ(writer の「紙は白のまま」と同じ考え)。
*これで calc は 145/145、リボンの灰色ゼロ*。
calc の AI タブ(発注者「AI メニューを実装して」2026-08-04): writer のui::ai(手元 / Claude 定額=claude CLI / Claude API の3宛先。
鍵は書かない)をそのまま使い、calc 流に10ボタン。
CalcAi は選択の TSV を渡し TSV/式で受ける —要約=コメントへ、書き直し/敬語/やさしく/翻訳=文字のセルだけ置換(行数が合わねば断る)、ふりがな=1列選ぶと右隣へ、続き=下の空きへ、表にする=パネルの文章をカーソルへ、頼む=パネルの用件、=で始まる答えは式としてカーソルへ・他はコメントへ。
反映は checkpoint 済み(Ctrl+Z 1手)、保護中は断る、空きでない置き場・行数の食い違いは断る。
*calc 155/155 でリボンの灰色ゼロ*。
両アプリの窓の移動・終了確認(別スレッド)・スクロール・クリップボード。
pysheet(Python 束縛、polars 連携)。

== 次の仕事

**英語版(発注者指示 2026-08-04「動くようにして」)— 材料が元の機械にある。
**この機械には vendor/web-apps が無く、`python3 ui/gen_ribbon.py en` は「現物が見つかりません」で止まる(実測)。
勝手な訳語で埋めるのは「名前は本家から取る」に反するのでやらない。
段取り: ① *元の機械で* `python3 ui/gen_ribbon.py en > ui/src/ribbon_en.rs` を生成してコミット(45言語のうち、まず en の表だけでよい) ② こちら側で切り替えの結線 — ribbon::WRITER/CALC の参照を「設定(language)を見て表を返す関数」に替える(settings.toml の器と同時にやるのが早い。
器ができるまでの繋ぎは OFFICE\_LANG=en の環境変数可) ③ 状態行・パネルの文言は当面日本語(SEKKEI「設定 — 器と言語」の段階③)。
詳細設定ページにその旨を正直に出す①が届けばこの機械側(②③)はすぐ動ける。
あるいは vendor/web-apps を元の機械からコピーしてもらえれば、①ごとこちらでやる。

*両担当へ(発注者 2026-08-04): 設定の器と言語。* 正本は SEKKEI「設定 —器と言語」。
`~/.config/officework/settings.toml` + ファイルタブ「詳細設定」ページ(共有 ui/ に器を作り、両アプリから使う)。
環境変数は一時上書きに降格(優先順: env > toml > 既定)。
言語は段階制 — ①器 ②リボンの言葉の実行時切り替え(gen\_ribbon を「ロケール→表」の埋め込みへ。
*45言語の表の生成は vendor が生きている元の機械で*)③状態行・パネルの文言の langへの外部化(数百句 — 一番大きい。
済むまで「状態行は日本語」と正直に)。
インターフェイステーマの灰色ボタンはこの器で実体を得る。
名乗り(ロック・チャット・署名の user@host)も設定に。
*設定が入ったらマニュアルの環境変数の記述を差し替える*(マニュアル担当が追従する)。

*AI タブは writer 側で実装済み(2026-08-04)。* 宛先の層は``lang::ai``(共通)— *定額の契約で使える道を用意した*(発注者指定): ①手元のモデル ②**Claude(定額)= 手元の claude コマンドを子で呼ぶ。
鍵は要らない** ③Claude(API。
鍵は環境変数だけ)。
選んだ宛先は~/.config/officework/ai.txt に覚える(JO\_AI が優先)。
CLI には道具を与えず(--allowed-tools 空)、文書は標準入力で渡す。
writer のボタンは10個(宛先・要約・書き直す・敬語・やさしく・翻訳・ふりがな・続き・表・頼む)。
*calc 側の AI タブと VBA の移し替えは calc 担当の受け持ちのまま* —宛先は lang::ai をそのまま使えば定額の契約でも動く。

**calc 担当へ(発注者確定 2026-08-04): AI タブと「VBA の移し替え」第1号。
**設計の正本は SEKKEI「AI メニュー」の節(「AI タブは作らない」の改訂 —校正はレビューに残したまま、モデルに任せる変換と生成の道具箱を作る)。

* リボンに *AI タブを両アプリに追加*(ribbon.rs + gen\_ribbon.py のAPP\_TABS/EXTRA\_CMDS 方式で二重書き。writer 側のボタンは当面灰色置き)
* *第1号 = VBA の移し替え(calc)*: xlsm の xl/vbaProject.bin からコードを取り出し(裏方 oletools — .venv に pip で足す。公共財)、モデルが **Python in Calc の台本(b/s 束縛)**へ書き換え、@save と同じ形で book.scripts に載せる。*人が確かめてから使う* — 変換結果は自動実行しない。元の vbaProject は原文持ち越しのまま消さない
* *宛先の規律*: 既定はローカル(校正と同じ OpenAI 互換の口)。外部(Claude API 等)は設定で明示したときだけ・鍵はブックに入らない・繋がらなければ「できません」(黙って空にしない)。手引きへの反映はマニュアル担当がやる(実装が入ったら教えて)

*済(2026-08-04)*: calc の画面の組み立ては第2弾まで写した — 一段の絵ボタン(汎用の描き方: タブの並びから自動で組み、BIG 表のボタンだけ名札つき大ボタン。
ホームは2段に割る。
乗ると名前が下のステータスバーへ = hover\_hint)、ファイルタブは全面ページ(品書き+ブックの情報。
統計は生きた値、プロパティは docProps/core.xml と**往復** — Book.props。
読みは grab、書きは patch\_core\_props(持ち越しの core.xml へ差し替え。
無い・新規なら部品と Content\_Types と .rels の宣言ごと足す)。
編集はパネル(prop-_)で、writer と同等になった)。最近開いたは recent-calc.txt 12件。新規作成 = new\_book(未保存があれば断る)。ついでの実バグ修正:罫線の辺が border\_color(全辺1色)で他の辺を巻き込むのを、辺ごとの重ね描き(1px の線)に直した(0b94b97 — writer も同じ穴が無いか一見の価値)。_

発注者の確定(2026-08-03): **このソフトは事務様式専用ではない(汎用)。
きちんと動作するボタンは灰色で残さず普通のボタンにする。
**挿入系=画像を貼る、コメント=段落単位、透かし・ページ色は実装済み。

**writer の HTML(発注者確定 2026-08-04)— 閲覧・記入・公開 Web まで済み。
**設計の正本は SEKKEI「writer の HTML — JS なしの閲覧と記入」。

* *できていること*: `kumihan::html`(自作の部分集合パーサ。約400行)で .html を文書として開く — 見出し・段落・書式・箇条書き・表・HTML のruby(うちのルビへ)・title(文書の情報)・実体参照。p/li/tr/td の閉じ忘れは癒す。*script は丸ごと読み飛ばす(JS は実行しない)*。文字コードは UTF-8 → CP932(encoding\_rs)。form は記入のパネルで書いてGET/POST(urlencoded)、答えの HTML をまた読む。リンクのパネルから辿れる。http と https の両方(rustls)、転送(3xx)も5回まで追う。*網の線引き*: 読みは公開 Web まで広く、*書き(送信)は開いた起点だけ*。Cookie は持たない。保存は docx(HTML 書き出しは作らない)
* *見立ては当たった*(発注者「それほど難しくない」— 同意していた): HTML→モデルの写像はほぼ1対1(h1〜h3=Heading、p=Body、table=w:tbl、ul/ol=箇条書き、b/i/u=CharFormat)で、*モデルに乗れば画面・PDF・docx 保存はタダで付いてきた*。html5ever も要らなかった
* *受入試験*: sample/html の11件(閉じ忘れ・CP932・宣言なし CP932・js・form を含む)と sample/catalog\_server.py(注文書 form 22欄の抽出とPOST の往復・リンク辿り)で実地確認済み
* *残る仕事(writer 担当へ)*: ①**ページに載った Python をサンドボックスで回す**(`<script type="py">` 等。発注者確定「Pyodide / PyScript もやる」のwriter 側の含意。**明示の操作でだけ**動かし、開く=実行は不変。実装は calc の run\_python\_inner 一族がそのまま使える。約束するのは「サンドボックスで回すこと」と「form 束縛(記入欄の名前→値の辞書を渡し、返った値を紙面に書き戻す)」だけ — *PyScript の API 互換は目標にしない*(発注者「こだわらない」)。DOM は持ち込まない) ②本文中の記入欄の直接編集(いまはパネル)③ページ内の別ホストの画像・CSS
* *向きの確認(発注者)*: これは ONLYOFFICE Docs とは別の方向。あちらは「オフィスを Web の編集器で実装する」、こちらは逆で *Web を文書として読む・記入する*。手本は「HTML→docx 変換器」ではなく「文書として組んで見せ、form を書けるようにし、POST する」こと(ブラウザ化の坂には登らない)

*K4(日本語一級)の進み(2026-08-04 発注者「やってみて」)*:均等割付(w:jc distribute 往復・最後の行も配る・画面/PDF とも)とルビ(CharFormat.ruby・w:ruby 往復・LibreOffice で実地確認済み。
*w:ruby の親は w:r* — 段落直下だと LO が基底ごと落とす、を踏んで直した)は実装済み。
*縦書きも実装済み*(2026-08-04。
fold\_vertical が行を右からの列へ写す。
約物は縦用字形へ。
ホーム > テキスト方向で入切、sectPr の tbRl と往復。
初版の限界 = 表・段組みは横のまま、ASCII は縦積み、変換下線なし — SEKKEI「日本語一級(K4)」参照)。
*K4 これで3点とも揃った。*

*writer は灰色ゼロ(114/114。2026-08-04)。* 残っていたコンテンツコントロール(sdt)+フォームタブ10・配色・複数ページを実装。
設計は SEKKEI「writer の灰色ゼロ」。

*記入欄の「名前」とマクロの fill(2026-08-05 発注者「記入項目に名前を付ける、これがフォームの作り方」)*: フォームタブに「名前」ボタン(11個目)—カーソルの欄の w:alias/w:tag をパネルで打ち替える。
マクロの前置きにfill(名前, 値)/fill\_one(writer の macro\_script)— w:tag で欄を引いて記入、無い名前は例外で断る。
独自種類の印とは「jo:email:名前」に合成(kumihan::split\_tag と ooxml が対)。
台本の作法は SEKKEI の記入欄の節。
2026-08-05 続き(発注者「基幹現代化への対応 1〜2 を形にして」):前置きに extract(名前)=値を読む と fields()=(名前, 値) の一覧 を追加。
fill=出口 / extract・fields=入口 の対(SEKKEI の記入欄の節)。

2026-08-05 さらに(発注者の docxtpl 提案 →「すすめて」): *雛形の車線を追加*。
前置きに render(辞書)= docxtpl の {{名前}} / {%tr %} 差し込み、tpl\_fields()= 差し込み口の一覧。
ooxml::read の出口に kumihan::heal\_runs (同書式の細切れ run を読みで繋ぐ — モデルの衛生+雛形の {{ }} の保険)。
docxtpl 0.20.2 を .venv に追加(薄い包み。
止まっても自前で持てる規模)。
実測: run 分断3種(同書式・太字違い・波括弧の途中)は docxtpl が自力で繋いだ。
壊れ方の本命は {%tr %} の置き方(行ごと構文になる — for/明細/endforの3行構成が正)と全角の{{ }}。
render の断り文言にその旨を入れてある。
試験: 雛形の一本通し(分断→writer 読み書き→render→表の行くり返しまで)+実物様式7件(機構)が writer 読み書き→python-docx/docxtpl に通ること。
AI への system も実装済み(2026-08-05「続けて」): AI タブ11個目**「マクロを書く」(ai-macro)** — 台本の作法を system で教え、記入欄の名前一覧(sdt\_names)を的として渡す。
答えは plugins 置き場に .py で置くだけ(自動では走らせない)。
calc 側の AI タブに同種の口はまだ無い — calc 担当へ(Python in Calc の台本を書かせるボタン。
b/s の作法は SEKKEI 参照)。
実物様式の一本通しは**済**(2026-08-05「進めて」): 様式1(参加表明)は表なし・ラベル段落型で、住所/商号/代表者の**同じラベルが3組**ある —名前(代表・住所 / 協力1・住所 / 担当・氏名 …)が要る実例そのもの。
試験「実物様式1で名前付けから吸い上げまで通る」が、13欄の名前付け(印で終わる段落は印の前に挿す)→ fill(同名でも誤爆しない)→ extract → 欄の生存、まで実物で回している。
発注者が GUI でやるときもこの名前の付け方(節・項目)が手本になる。

*calc の5つの直し(2026-08-06 発注者報告)*: ①窓の縁のつかみ(ui::resize\_edges — writer にも入れた。
mutter は SSD 非対応で外枠が出ない) ②パネル(prompt / ソルバー)は表の中央 ③折り返しOFFは1行+隣の空セルへはみ出し描画+縮小して全体を表示(shrinkToFit 読み書き・画面とも) ④**書式は本当に据え置き**(保存が styles.xml を作り直していた破れ目を据え置き+追記合成 styles::append\_to で塞いだ。
Sheet.style\_of が索引を控え、触っていないセルは原本の索引のまま。
実物様式7の試験で「開いて保存=無変化」を固定)⑤新規は空白のブック。
設計は SEKKEI「calc の5つの直し」。

*writer の画面(2026-08-04。発注者が画像で指示)*: デスクトップ版の画面の組み立てに合わせた — クイックアクセス行+下線タブ+下のステータスバー、ホームは2段の絵ボタン、挿入と残りのタブは一段(名札つきの大ボタン+絵だけのボタン)、ファイルタブは全面のページ(品書き+文書の情報。
プロパティはdocProps/core.xml と往復)。
*左パネル*(見出し/コメント/検索の3つのタブ。
押すとその場所へ飛ぶ)と**右パネル**(いる場所の文字・段落・ページの設定をその場で直す盤。
ボタンは run\_cmd を呼ぶだけなので実体が二重にならない)は本物を作った — 最初は既存のパネルの入切に割り当てただけで、発注者の「作りましたか」の一言で作り直した。
*入切のボタンは入っていると枠と色が付く*(toggled(id)。
「押したのに何も起きない」に見えるのを仕組みで防ぐ)。

*メニューの動作試験(2026-08-04)*: `writer/src/main.rs` の``menu_run_tests`` が GUI を起こさずに(gpui の試験の場で)*リボンのボタンを全部おす*。
空の文書と sample/writer の5件の両方で回し、落ちないこと・未配線が無いこと・入切が2度で戻ること・主なボタンが本当に文書を変えること・フォームのボタンが記入欄を入れることを見る。
*ファイル選択の窓を開く12個だけは試験から外す* — rfd は画面の無い試験では返ってこない(踏んで確かめた。
実機での確認に回す)。

*calc にも同型のメニュー動作試験(2026-08-05、発注者「サンプルを作成して、メニューの全件チェックをして修正して」)*: calc/src/main.rs の menu\_run\_tests。
全ボタン(145)・入切8個の往復・見本5冊(sample/_.xlsx)で回す。踏んだ穴:_

* freeze はカーソル A1 では効かない仕様(固定する位置が要る)— 試験は B2 で押す
* *data-external-links が rfd の pick\_file を背景のスレッドで開く* — 画面の無い試験では永遠に返らない(gdb のスタックで特定)。DIALOG 除外は7個。rfd 呼びが関数化されていないと走査から漏れる — **rfd を増やしたら試験の DIALOG 表も見る**こと
* 見本は temp への**写しを開く** — 署名(.sig)やチャット(.chat.txt)が隣にファイルを添えるので、追跡している sample/ を汚さない
* 発注者「リボンでサボっているのは無かったか」(2026-08-05)→ 全アームを走査。status を書くだけの空ボタンはゼロ、パネル(prompt)と一覧(pick)の受け手も全部揃っていた。*サボりは1件だけ*: calc のリボン「すべて選択」が常に数式バーの文字選択で、セルに効いていなかった(Ctrl+A の a\_select\_all とは別実装だった)→ select\_all\_now に実体を寄せて両方から通し、「全選択はセルに効く」試験を追加。あわせて試験の seed が sync\_input を呼ばず、最初の commit() が A1 を空で潰していたのも直した
* サンプルは全部生成スクリプトで作り直した(writer 5件はバイト一致=生成スクリプトは決定的。14件とも rtall 往復「一致・注記0」)。.venv に matplotlib と scipy を追加(scipy・mathtext の台本試験は環境の欠品で落ちていた。コードの不具合ではない)

*窓の位置と大きさの控え(2026-08-05、発注者「起動時の位置やサイズも保存できているか確認しておいて」→ 未実装だったので実装)*: ui::winstate。
~/.config/officework/window-{writer,calc}.txt に1行 `x y w h [max]`(recent と同じ作法)。
開くときに読み(壊れた・常識外の控えは黙って捨てて既定の中央へ)、cx.observe\_window\_bounds で動かす・伸ばすたびに書く — 閉じる経路が何本あっても漏れない。
全画面は控えない(次も全画面で開くと出口が分かりにくい)。

* 実機確認は headless mutter + rootful Xwayland(:77)+ python-xlib の一巡(移動 → 伸縮 → 落として再起動): writer も calc も控えどおり復元(X11)。x が +2 ずれるのは gpui X11 の癖(CreateWindow で +2 している)。累積はしない
* gpui X11 は**サイズが変わる ConfigureNotify の座標を信用しない**(親相対の罠対策)。移動と伸縮が別イベントなら正しく追う — 実運用の WM はそう出す
* *Wayland は位置の復元が効かない*(プロトコルが客に位置指定を許さない)。大きさは効く。控えが壊れることもない — そういうものとして受け入れる

*pysheet: datetime の受け口(2026-08-05、Fable の指摘「帳票では日付セルへのdatetime 代入が頻出」→ 発注者「まず、これをやるのがいい」)*: `s["A1"] = datetime.date(2026, 8, 5)` が Excel の通し番号になる(date / datetime / time の3種。
datetime は date の子なので判定順に注意)。

* 通し番号の規約は sheet::calc::date\_serial に**一本化**(DATE 関数と pysheet が同じ道を通る。別々に持つと必ずずれる)。test.py は =YEAR(G1) で規約の一致を検査
* セルに表示形式が無いときだけ yyyy/m/d(時刻は h:mm)を付ける。帳票の日付セルには元の形式が付いているので、それに従う(据え置きの原則)
* pyo3 0.29 + abi3 の罠: PyDateAccess/PyTimeAccess は limited API では**存在しない**→ 属性(year/month/day…)の getattr で読む。downcast は cast に改名されている
* 置けない型は TypeError で型名を言って断る(黙って str にしない)
* 既知の残件: format\_value が日付書式(yyyy 等)を描けないので、display() とcalc の画面では通し番号のまま見える。ファイルは正しいので Excel では日付で出る。書式の描画対応は関数拡充(TEXT)と同じ土俵でやるのが良い

*関数拡充 第1段(2026-08-05、発注者「このcalcは、業務専用だとは思っていません」→「これをやりませんか?」)*: 47個 → *84個*。
日常と帳票を閉じる37個を追加:

* 条件集計: SUMIFS/COUNTIFS/AVERAGEIF/AVERAGEIFS/MINIFS/MAXIFS(範囲の大きさ違いは#VALUE!。1件も合わない MINIFS/MAXIFS は 0 = Excel の約束、AVERAGEIF は #DIV/0!)
* 選ぶ: IFS/SWITCH/CHOOSE(*選ばなかった枝のエラーは踏まない* — IF と同じ扱いでエラー素通し表に追加)、XLOOKUP(完全一致。見つからない時の第4引数あり)
* 日付時刻: TIME/HOUR/MINUTE/SECOND/DATEVALUE(和文「2026年8月5日」も読む)/ EDATE/EOMONTH(月またぎは civil の 13月正規化で処理)/DATEDIF(Y/M/D/YM/MD/YD)/ WORKDAY/NETWORKDAYS(土日+指定休。step 上限で暴走を止める)
* 文字列: TEXT/SUBSTITUTE(n個目指定可)/FIND/SEARCH(1起点の**文字**番号。バイトでない)/ VALUE(¥と桁区切りと%を解く)/TEXTJOIN(Text("") も空と見る — is\_empty はValue::Empty しか見ないので display() で判定)/REPT(32767字上限)/CHAR/CODE
* 位置: ROW/COLUMN/ROWS/COLUMNS — 参照を**値に崩す前に**読む必要があり、P(評価器)に at: Pos(いま計算しているセル)を足して atom で横取り
* 順位: RANK(同値同順位)/LARGE/SMALL、SUMPRODUCT
* *TEXT のために format\_value に日付書式の描画を実装*(model.rs format\_date)。これで display() と calc の画面でも日付セルが日付で見える(前日の残件解消)。m の月/分は「h の直後か、次が s なら分」で判定。aaa=曜日(日本語)。# や 0 が混ざる書式と和暦(g/e)は数の道へ(和暦は次の課題 — 黙って崩さない)
* 罠: 既存試験「知らない関数は名前エラー」が XLOOKUP を『無い関数』の例に使っていて、実装したら落ちた。実装が進むたびこの試験の例は差し替えること
* 関数一覧のボタン(fn-_)も同じコミットで更新(「できないものを、できるように見せない」の逆 — *できるようになったものを一覧に載せ忘れない*)*第2段も同日に完了(発注者「第2段階に進んで」)*: 84個 → *129個*。統計・数学・情報の45個: MEDIAN/MODE/STDEV(P)/VAR(P)(新名 STDEV.S 等も別名で受く)/ PERCENTILE/QUARTILE(直線補間 = .INC 同等)/CORREL/SLOPE/INTERCEPT/FORECAST(対で見る統計は pre-flatten で形を保つ。両方が数の行だけ使う)、FACT/COMBIN/PERMUT/ GCD/LCM(あふれは checked\_mul で #NUM!)、三角・双曲線・指数対数(PI SIN〜LOG10 DEGREES RADIANS)、CEILING/FLOOR/MROUND/EVEN/ODD/SIGN(符号違いの基準は #NUM!)、RAND/RANDBETWEEN(xorshift64_ 自前 — 依存を増やさない。揮発性は NOW と同じ「recalc のたび」規則)、ISNUMBER/ISTEXT(エラーは FALSE — エラー素通し表へ)/ ISEVEN/ISODD/COUNTBLANK/SUMSQ。
* *字句の罠(重要)*: ATAN2 や LOG10 は「ATAN 列の 2 行目」とも読める。lex を「*直後が ( なら関数名*」に直した。関数名がセル参照に化ける事故はこの先も増える名前(例: 将来の DAY360 など)で必ず再発するので、この規則を消さない
* Excel の ATAN2(x,y) は数学の atan2(y,x) と**引数が逆順**(試験に明記)*第3段も同日に完了(発注者「第3段に進んで」)*: 129個 → *135個* + スピル。
* *OFFSET / INDIRECT* — パーサー階層で「参照そのもの」を計算(args では形つきのRect、式の中では1セルの値。複数セルを1セルの場所に置けば #VALUE!)。INDIRECT の別シート参照はまだ → 黙って自シートと読まず #REF!
* *再計算は fixpoint に*: 計算で決まる参照とスピルは1回の走査では依存の順が読めない → その式があるシートだけ「値が動かなくなるまで回す」(上限5周。RAND/NOW/TODAY 入りの式は毎回変わるので安定判定から除外)。無いシートは従来どおり1回(性能を落とさない)
* *動的配列(スピル)*: FILTER / SORT / UNIQUE / SEQUENCE をセル単独で書くとあふれて広がる(=PY と同じ「セル単独」制約。式の中に混ぜたら #配列単独)。
** 席の検査: 中身のあるセルへはあふれず #SPILL!(黙って潰さない)。起点は残し、途中まで書かない
** *掃除の順序が肝*: 前回の影を先に消すと、通常の式が「消された直後」を読んで縮んだまま安定する — 置き先を全部決めてから(掃除 → 書き込み)。影の席は「置き直してよい席」としてだけ先に覚える(freed)
** スピルの記録は Sheet.spills ↔ *xl/joSpill.xml*(joPivot と同じ独自部品)。これが無いと開き直しで自分の跡が他人のデータに見え、偽の #SPILL! になる(試験「spillの記録がxlsxを往復する」が保証)
* **範囲の要素ごとの比較**を実装(FILTER(A1:B9, C1:C9>100) の定番)。比較の規則は cmp\_values に一本化(式の比較と同じ道)*第4段も同日に完了(発注者「Excelで作ったxlsxファイルが読めないと困る」→互換の穴埋め)*: 135個 → *約180個(別名込み)*。実物のファイルで出現頻度が高いのに #NAME? になっていたものを狙った47個:
* *SUBTOTAL*(オートフィルターと「小計」機能が自動で埋め込む筆頭。101〜111 は 1〜11 と同じに扱う — この評価器は行の畳みを知らない正直な近似。AGGREGATE も同様に受ける)
* 新名・別名: CONCAT / RANK.EQ / RANK.AVG / UNICHAR / UNICODE / DBCS / CEILING.MATH / FLOOR.MATH(負の既定が旧 CEILING と違う — 0 へ寄る)/ TRUE() FALSE()(**括弧つき**も実物に出てくる)
* 情報: IFNA / NA / ISNA / ISERR / ISLOGICAL / ISNONTEXT / T / N / TYPE (エラーを受けて働くのでエラー素通し表へ)
* 古典: LOOKUP(値以下でいちばん大きいもの)、TRANSPOSE(スピルの5つ目)、ADDRESS / HYPERLINK(表示だけ)/ QUOTIENT / AVERAGEA / MAXA / MINA
* 日付: DAYS / DAYS360 / YEARFRAC(基準1は 365.25 近似 — 厳密式は残)/ WEEKNUM / ISOWEEKNUM
* 財務の反復解: *NPV / IRR / RATE*(符号の変わる区間を探して挟み撃ち = bisect()。「反復解は持たない」の方針はここで撤回 — 解けないときは #NUM!)
* 文字: PROPER / EXACT / CLEAN / FIXED / YEN / NUMBERVALUE
* *日本語系(うちの差別化)*: LENB / LEFTB / RIGHTB / MIDB(全角=2、半角カナ=1 — Excel 日本語ロケールの数え方。古い帳票の定番)、*ASC / JIS*(全半角。濁点は ASC で2文字に割れ、JIS で1文字に組み直る —往復の試験あり)、*DATESTRING*(和暦 令和08年08月05日。改元日の境まで試験)*上の残4件も同日に完了(発注者「続けて」)*:
* *和暦の表示形式*: format\_date が g/gg/ggg(R/令/令和)と e/ee(和暦年)を描く。元号表は calc::era\_of に一本化(DATESTRING と同じ道)
* *配列の入れ子*: SUM(FILTER(…))・COUNTA(UNIQUE(…)) 等、**関数の引数の場**は通る(args の try\_array\_arg。入れ子のスピル式 =SORT(FILTER(…)) も可)。裸の式の場(=SEQUENCE(3)+1)はまだ #配列単独 — 実装したら dan3 の試験の期待値を差し替えること(SUM(SEQUENCE) の試験は今回それで直した)
* *PHONETIC*: sharedStrings の rPh を読んで Sheet.phonetics(Pos→読み)に持ち、*保存で書き戻す*(前は「保存時に落ちる」と注記していた — 注記は消した)。PHONETIC 関数は読みが無ければ字そのもの(Excel の約束)。同じ字で違う読みの2セルは先勝ち(共有文字列表は字で引くため)
* *INDIRECT の別シート*: 器から作った — recalc(1枚)は残し、*recalc\_book(book, i)*(他シートを読み取りで見せる)と**recalc\_all(book)**(間接参照があれば2周)を新設。P に others、参照の答えは RefAns(At=自シートの範囲 / Rect=別シートの値の写し / Bad)。'台帳'!A1 の引用も剥く。*1枚だけの再計算では変わらず #REF!*(正直)。calc は機械置換32+手直し4(commit・開く3経路・外部リンク取り込み)、pysheet は recalc\_all / with\_calc(書き換え→ブック文脈で再計算)へ。単票のままの recalc はソルバー・ゴールシークの複製と新規ブックの見本だけ

*配列数式と演算子の組み合わせも完了(発注者「追加して」2026-08-05)*: =SEQUENCE(3)+1 のような式が要素ごとに計算されて隣へあふれる。

* 評価器 AP(配列数式用。文法は P と同じで演算が要素ごと)を新設。形が違えば大きい方に広げ、届かない要素は #N/A(Excel の配列数式と同じ)。1行・1列の側は引き伸ばす(ブロードキャスト)
* *関数の引数の解析を AP に一本化* — 旧 try\_array\_arg・try\_ref\_call・範囲比較の特例(C1:C9>100)は全部 AP に吸収。おかげでSUM(SEQUENCE(3)\*2) や SUM(A1:A3\*10) も通るようになった
* 1×1 に落ちる答えは普通の値として置く(スピルの記録を残さない)
* #配列単独 の断りは**役目を終えて撤去**(どの場でも評価できる)。配列数式の判定は「字句に ARRAY\_FNS の名前があるか」(文字列内の"FILTER" を拾わないよう、必ず字句にしてから見る)
* 範囲だけの式(=A1:A3\*2 で FILTER 等を含まないもの)は従来どおり先頭セルの値 — 昔の Excel の暗黙の交差と同じ扱い。スピルさせたければ実装済みの関数を1つ噛ませる。全面的に変えるかは設計判断として残す

*文書の二言語化(発注者「英語版を正に、日本語版を副に」2026-08-05)*: README と docs/ の3冊は**英語版が正**(元の名前 README.adoc / docs/_.md)、日本語版は副(README.ja.adoc / docs/_-manual.ja.md)。
相互リンクを各冊の頭に。
あわせて**独自語を標準的な言い方に直した**(発注者「独自の言葉は使うな」):ボタン→ボタン / パネル・小窓→ダイアログ / サンドボックス→サンドボックス / 網→ネットワーク /帳簿→レポート / 品書き→メニュー / 額縁→画面の組み立て(場所を指すときは「上の2行」のように具体に言う)/ シートのタブ→シートタブ /名乗り→名前 / 申し送り帳→チャットファイル / 台本→スクリプト / 錠→ロック /堰き止める→ブロック / 指図→定義 / 名無しの複製→名前のないコピー /添え書き→署名ファイル / 灰色→グレー表示。
*コードのコメントとHIKITSUGI/SEKKEI は対象外*(内部文書)— ただし発注者向けの文はこの表に従う。
calc の手引きの関数一覧(44個のまま古かった)は現状(約185・動的配列・別シート INDIRECT・PHONETIC)に合わせて書き直した。
新しい機能を足したら**英語版から先に**直すこと(正本はそちら)

*国際化 段階①②(発注者「国際化を行って」2026-08-05)*: リボンの言葉が実行時に ja/en で切り替わる。
SEKKEI「設定 — 器と言語」の段階どおり:

* *器*: ui::settings — ~/.config/officework/settings.toml の素朴な``key = "value"`` 読み(依存を増やさない)。優先順 env > toml > 既定。いまは language だけ。*詳細設定ページ(GUI)はまだ* — 器の吸収先(フォント・校正宛先・名前・世代数…)ともども残件
* *en の表*: gen\_ribbon.py は現行 ribbon.rs とズレていた(手で足したボタンが生成側に無い — 二重書きの実態)ので、**新しい生成スクリプト gen\_ribbon\_locale.py**を作った: 正=現行 ribbon.rs の構造、語だけを vendor/web-apps のja.json→<loc>.json 対訳で置換。本家に無いこちらのボタン(AI タブ等)はスクリプト内 OVERRIDES 表(未解決語が出たら**止まる** — 黙って日本語のまま出さない)。出力は ui/src/ribbon\_en.rs
* *切り替え*: ribbon::writer\_tabs()/calc\_tabs()(settings::language() を見る)。描画・当たり判定は active な表、**内部の論理(タブ名照合「保護」「ホーム」等)はja の表のまま**でよい — id・並び・ready・icon は全言語で同一(試験「各言語の表は語だけが違う」が固定。ここが配線の生命線)
* 名札の短い形(writer の LItem 上書き39個・calc の BIG 表)は ja 専用 —他言語では表の語に落とす結線を入れた
* 環境変数の実選択も試験(settings「環境変数でenの表が選ばれる」。OnceLock なので同プロセスの最初の language() 呼び出しであること)
* *正直な限界*: ステータスバー・ダイアログ・右クリック・ファイルページの文言は日本語のまま(段階③ = lang への外部化、数百句)。README の各国語版の節にもそう書いた。言語の選択肢は文言の揃った ja/en だけ**段階③と詳細設定ページも完了(発注者「これをやってしまいましょう」2026-08-05)**:
* *文言の外部化*: lang::i18n — **日本語の文がそのまま鍵**の t!()/tf!()。ja では鍵をそのまま返すので日本語の挙動は1バイトも変わらない。穴埋めは実行時の簡易整形({}・{:.0}・{:?} の3種だけ — format! は雛形が定数でないと使えないため)。en の表は lang/src/i18n\_en.rs(*585句全訳*)
* 変換は機械+要所: status 代入・format!(CJK)・"CJK".into()/.to\_string()・ .child("CJK")・mk のラベル・小ボタンのタプルを Python の変換器で一括(試験モジュールより上だけ。*calc の BIG 表だけ除外* — en はcmd.label への落とし込みで既に解決済みのため)
* *門番 ui/gen\_i18n.py*: t!/tf! の鍵を全部抽出し、未訳・不要訳・重複があれば**止まる**。文言を足したら --missing で骨組みを出して訳を書く。複数行リテラルは正規表現でなく字句走査で読む(1敗して直した)
* *詳細設定ページ(両アプリ)*: ファイル > 詳細設定(灰色だった席を実装)。言語の切り替え(settings.toml へ書く。*次の起動から反映* — language() は OnceLock)+ 実効値の表示(書体・校正宛先・Python・名前)。settings に get/set(他の行は保って1鍵だけ書き換え)
* 残: 器に吸収する設定の続き(フォント・校正宛先・名前・世代数を**編集できる**ようにする)、他言語(表2枚: ribbon\_<loc>.rs とi18n\_<loc>.rs を作れば増える — どちらも門番つき)

小さい残件: 変換下線の位置の実機確認、表入りのヘッダー・フッターの編集。
暗号化の Agile 方式(Word/Excel 2013+ の既定)を実装(2026-08-04、発注者「作れますか」→「やって」): ooxml/src/crypt.rs の後半。
SHA-512 の鍵導出をspinCount 回(10万)+AES-256-CBC の 4096 バイト区分+HMAC-SHA512 の完全性。
base64・HMAC・CBC(ECB の器で連鎖)・XML 属性引きは依存を増やさず自作。
decrypt() は minor==4 を見て agile\_decrypt に振る(呼び側は無変更)。
*本物と相互検証*(.venv の msoffcrypto + python-uno の LibreOffice で):自作の出力を msoffcrypto が解け、LibreOffice が書いた本物を自作が解いた結果は msoffcrypto と1バイト一致。
calc の暗号化保存は encrypt\_agile へ切り替え(AES-256。
読みは Standard も Agile も両対応)。
*writer も encrypt\_agile に替えた(2026-08-04。申し送りは解消)* —保存は AES-256、読みは Standard も Agile も両対応なので前に作ったAES-128 の文書もそのまま開ける。
自作の出力を msoffcrypto が解けることをwriter 側でも確認済み。
※罠: msoffcrypto の encrypt は壊れた CFB(宣言長と実体長が食い違う)を書く道具なので、書きの検証には使えない — 読みの参照専用。
書きの検証はLibreOffice(python-uno、自分のプロファイルと別ポートで起動)で。
(「すべて置換」の書式均しは 2026-08-04 に解消済み — 1置換ごとに写す)

*多言語化を一気に実施 — 14言語に(2026-08-05、発注者「多言語化は一気にやってしまいましょう。プログラムを作って」→「claude の定額内のSDKでやって」)*:

* *訳は API のクレジットを使わず、定額内のサブエージェント(Claude Code のAgent)で作った*。発注者がプロモーションクレジットの話の途中で「定額内で」と指示 — API に curl する道は取らない
* プログラムは *ui/gen\_lang.py*(言語を1コマンドで増やす): --todo が材料 ui/i18n/keys.json(660句 = 文言585 + 独自ボタン75、番号つきja/en 対訳)を出す → 訳を ui/i18n/<loc>.json([{"i":番号,"t":"訳"}])で受ける → `gen_lang.py <loc>` が検査(欠け・空・穴埋め{}の不一致で停止)し、i18n\_<loc>.rs + ribbon\_<loc>.rs を生成、登録簿(lang/src/i18n\_tables.rs・ui/src/ribbon\_tables.rs・両 lib.rs の gen\_lang:begin〜end)まで書き換える。--validate は生成せず訳だけ検査(並列翻訳の作業用)、--check は登録済み全言語の検査。*表の揃わない言語は登録しない*(方針)
* 13言語を登録: en de es fr id it ko pt ru tr vi zh zh-tw(+鍵の ja で14)。cargo test --workspace 20組全部緑(リボンの構造試験「各言語の表は語だけが違う」が全言語を舐める)。README 両版の各国語版の節を更新(機械翻訳である旨と母語話者の校閲歓迎を明記)
* *サブエージェントの罠(次回のため)*: 12並列・4並列の一斉投入は途中で黙って死んだ(通知は「completed」でも実は中断 — 通知は「止まる度」に飛ぶので完了の証拠にならない)。*効いた段取り*: 1言語 = 660句を4つの部分ファイル(<loc>.part1〜4.json、165句ずつ)に分けて書かせ、python で結合 → --validate が通るまで直させる → part を消させる。死んだエージェントは SendMessage で再開できる。検証前に信用しない
* vendor(Euro-Office)のロケールに無い標準ボタンの語は言語ごとに違う — gen\_ribbon\_locale.py の OVERRIDES[<loc>] に手で足す(zh-tw:2語、it:7語、tr:18語、vi:73語、id:10語)。gen\_lang.py は材料の訳と OVERRIDES を併合
* *既知の課題*: calc/src/main.rs の並べ替えの文言(i=268 相当)が「大きい値」「小さい値」を**実行時の引数**として渡すため、ja 以外では文の中にこの2語だけ日本語が残る。直すには鍵の増減が要るが、材料が番号方式なので**番号がずれる** — 将来「ja の文そのものを鍵にした訳ファイル」方式に移行するのが本筋(gen\_lang.py の材料を番号でなく鍵で持つ)
* 残: 言語別の組版・校正(lang の Language trait は ja/en のみ実装)。リボンの短縮ラベル(BIG/LItem)は ja 以外 cmd.label に落ちる(en と同じ扱い)

== 次の仕事 — calc の灰色9つ(2026-08-12 調べ)

*「完成」を数で置くなら、ここが唯一の候補。* アプリ自身が「まだできていない」と宣言しているのはこの9つだけで、writer は既に灰色ゼロ。
台帳は歴史が多いので、**いま残っているものは灰色の数がいちばん正直**だった。

模型と突き合わせた結果、*7つは結線だけ*:

[cols="1,1,1"]
|===
|タブ |ボタン |要るもの

|レイアウト |前面へ移動・背面へ移動 |`shapes: Vec` の並べ替え。*模型は既にある*(順序 = 重なり順)
|レイアウト |配置 |座標を揃えるだけ
|保護 |ブックを保護する・範囲を保護する |`Sheet.protected` と `ProtectAllow` が**既にある*
|表示 |標準・改ページ プレビュー |`show-breaks` `pagebreak` が cmds.rs に**既にある**。表示の切り替え
|*レイアウト* |*グループ化* |*模型が無い*(図形の組を持つ必要)
|*レイアウト* |*図形を結合* |*幾何の演算*。これだけ別格
|===

*始める前に `cargo test -p calc -p writer` で基準を取ること* — と書きかけたが、`d1d120b` で CI に載ったので、いまは push すれば機械が見る。
基準は2026-08-12 時点で *calc 151 / writer 38*。

== 残件(2026-08-12)

* *`ui/gen_ribbon_locale.py` と `tools/ribbon_sweep.py` は、まだ自前の正規表現で``ribbon.rs`` を読んでいる。* `177eb63` で解析器を `tools/ribbon_parse.py` に集めたが、この2つは切り替えていない。理由は**確かめられないから** —生成器の受け入れ試験(14言語を作り直して `git diff` が空)は ``vendor/web-apps``が要り、この機械には無い。*出力を比べられない生成器を差し替えるのは、静かな差分を入れるやり方。* 材料のある機械で切り替えて、受け入れ試験を回すこと
* **CI の「画面のいる calc / writer」の仕事は、GitHub のランナーで未検証。**apt の一覧は組み上がったバイナリの `ldd` から起こしたが、実際に走らせていない。最初の赤が足りない物を教えるはず
* *`tools/wiring_check.py` を畳むかどうかは期限つきの宿題。* `wiring_tests` がCI で走るようになったので内容は重複している。**遅い仕事が数日続けて緑**なのを見てから畳む。忘れると二重に保守し続けることになる

== この日の教訓(まとめ)

*「検査が緑なのではなく、検査が見ていないだけ」*(fork セッションの言葉)。
2026-08-10〜12 で同じ形を十数回踏んだ:

* crc32 だけ見て**圧縮後の大きさ**を見ていない検査 → 詰め直しを見逃す
* 型紙が**繰り返しだけ**で、どの圧縮水準でも同じに縮む → 検査が空を打つ
* **入れ物の名前**を数えて中身を見ない(`xl/drawings/` にコメントの VML)
* 試験の schema が `passthrough()`、本番が `strict()`
* CI の対象から `calc`/`writer`/`ui`、`-p sidecar` が漏れる
* 字面の検査が**拾う**形 → 拾えなかった物は黙って消える
* *「実害なし」の判定が、実害を見る道具を塞いでいた*(`styleIndex`)

対になる作法:

* *検査を信じる前に、わざと壊して鳴らす。* 鳴らない検査は無いより悪い
* *拾うのではなく食べ尽くす。* 残りが出たら落ちる形にすれば飛ばせない
* *数を報告するなら、その場で測る。* 記憶から出した数は必ずずれる
* *まとめて直すときほど、1件ずつの意味が薄まる* — clippy 41件を機械的に片付ける速さの中で、「押しても何も起きないボタン」を1つ作った
