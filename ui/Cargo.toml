[package]
name = "ui"     # writer と calc が共有する入力の結線(IME含む)。UIの共通部品はここだけ
version = "0.0.1"
edition.workspace = true
license.workspace = true

[dependencies]
kumihan = { path = "../engine" }
# 絵を描かない物(リボンの表・14言語の札・キー・設定)。gpui を持たない
face = { path = "../face" }
lang = { path = "../lang" }
pyrun = { path = "../pyrun" }
# GPUI は crates.io に無いので、版を固定して zed から直に取る
gpui = { git = "https://github.com/zed-industries/zed", rev = "5e1fd392f67e27fa1da91bad43eef7db1a5dec23" }
resvg = "0.48.1"

[dev-dependencies]
ooxml = { path = "../ooxml" }
