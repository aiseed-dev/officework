# office — ネイティブ Rust の日本語オフィス(設計)

## 目的は2つ、向きが逆(2026-08-13 発注者確定)

**第一: Python を用いて Office を自動操作する**(Python → Office)。
他のすべて(2つのアプリ・エンジン・互換の顔)はこの線の上に並ぶ:

- **エンジン**(`pip install officework`)が主役 — xlsx / docx を書式ごと
  壊さずに読み書きし、openpyxl・python-docx・xlwings の書き味を継ぐ
  (0.2.0 の「三つの顔」がこの線)
- **writer と calc は、自動化の結果を見ながら整える面** — 人が確かめ、
  手で仕上げ、起動中のブックを Python から直に操る相手にもなる
  (ops の言葉は Python もアプリも同じ意味で使う)
- ayumi の目標「移行の壁を消す」との関係: 壁の最大の物は VBA と手作業の
  帳票運用で、その置き換えの答えが Python 自動化。だから「ブックはコードを
  運ばない・コードは .py」も、この目的の形そのもの

**第二: Office から Python を利用する**(Office → Python)。
セルの式が plugins の Python 関数を呼び(`=倍(A1)`、2次元はスピル)、
パネルの `@名前` が手続きを回し、グラフ・ピボット・ソルバーのボタンの
裏で matplotlib・polars・scipy が働く — Python in Calc の側。

**出自**: 発注者が 2018-12-24 に書いた記事
「[ExcelにPythonが搭載?その後 — xlwings を使おう](https://qiita.com/yniji/items/b38bc312e860027108ac)」。
Microsoft の「Excel に Python」は実現せず、記事は xlwings で
**「Excel から Python を利用する」ことを「メインの使い方」**と呼び、
マクロと UDF(動的配列で複数セルへ)を勧めた。officework はその答え合わせ —
8年後に自分の calc へ本搭載した(python.ja.md「UDF とスピル」、
続編は docs/articles/excel-python-sonogo-2026.ja.md)。

以下の各節(2本のアプリ・ネイティブファースト…)は目的ではなく**手段の
決め**として読む。

## Office を一つのソフトにしない(2026-08-02 確定)

文書は **writer**(docx互換)、表は **calc**(xlsx互換)。**別のバイナリ**にする。
共有するのは書式(docx/xlsx)と核(kumihan/sheet)だけで、アプリは分ける。
巨大なスイートにしないほうが、小さく作れて検査もできる。

範囲は「Euro-Office ができている程度」で十分。凝った機能は要らない。
**VBA 型のマクロは実装しない** — 「開く=実行」という攻撃経路を持たない
(aiseed-migration-kit DESIGN.md §5 と同じ思想)。コードの持ち運びは
Python in Calc が担う(2026-08-04 改訂): ブックにコードを**載せられる**が、
開いても決して自動実行せず、実行は明示の操作+サンドボックス(サンドボックス)の中だけ。
詳細は「Python in Calc」の節。

**↑の Python の姿は改めた(2026-08-09)** — いまは**ブックはコードを一切
運ばない**(`@save` も joPython.xml への書き込みも廃止)。コードは
`~/.config/office/plugins/*.py` にだけあり、セルは `=倍(A1)` の普通の
関数呼び出し。正は [docs/sekkei/python.ja.md](docs/sekkei/python.ja.md) の
「↑をさらに狭めて」の節。「開く=実行を持たない」という芯は変わっていない —
むしろ「載せられるが走らせない」から「載せられない」へ強くなった。

## 前提の確定(2026-08-02)

- Euro-Office のデスクトップ版(DesktopEditors)は存在するが**遅い**。
  遅さはバグではなく構造 — JSエディタ + Chromium という Web スタックそのもの。
  シェルだけ Rust にしても(desktop-sdk の置き換えでも)この遅さは継承される
- **Zed が証明したこと**: 「Rustで書けた」ではなく、**速さは構造で決まる**。
  GPU直描きのUI(GPUI)、ロープ構造の文書、ネイティブのテキストシステム。
  GPUI は Apache-2.0 で公開されており、その土台の上に建てられる
- したがって蒸留の最終形は、シェルではなく**オフィスそのもの**をネイティブで書くこと

## ネイティブファースト(2026-08-07 発注者確定)

本家(Euro-Office)は自らの価値を「ブラウザファーストの体験」に置く。
**うちは逆** — ブラウザーファーストではなく、**ネイティブファースト・
デスクトップファースト**でやる。これは上の「前提の確定」の帰結であり、
先方の看板文句を読んだうえでの明示の選択。

具体的には:

- **速さと手応えが第一。** GPU 直描き・即起動・キーボード一級・IME 一級。
  ブラウザ由来の層(webview/Electron/JS ランタイム)は今後も持ち込まない
- **本家合わせは「画面と挙動」の写しであって、実装の写しではない。**
  vendor/ は挙動の教師のまま(リボンの並び・文言・ダイアログの形を学ぶ場所)
- **ローカルファースト。** 文書はファイル、設定は ~/.config のファイル、
  共同の印はロックファイルと言伝ファイル。口座もサーバーも要らず、
  網が無くても全部動く(網を使うのは明示の操作だけ — @更新 net など)
- **統合は「40のWeb統合」ではなくデスクトップの流儀で。**
  ファイルそのもの・Python(サンドボックスつき)・CLI・ローカルのプロセスが統合点。
  配布も OS の流儀(.deb / 公証つき .dmg — 「office の配布方針」参照)

(↑の「サンドボックスつき」は 2026-08-09 に姿が変わった — 必須の鎧ではなく
なった。現状は [docs/sekkei/python.ja.md](docs/sekkei/python.ja.md))

## 構成(Zed との対応)

| Zed | office | 状態 |
|---|---|---|
| rope / text system | **kumihan**(組版エンジン: 文書モデル + JIS X 4051 行組み) | K0 実装中 |
| GPUI(GPU UI) | GPUI 採用が第一候補(代替: wgpu + vello/parley 系) | K1 で可否検証 |
| tree-sitter / LSP | OOXML 読み書き(docx⇄自前モデル) | K3 |
| — | **日本語一級**: 縦書き・ルビ・均等割付(存在理由) | K4 |
| — | 表・様式(罫線帳票)・印刷。yoshiki(様式プロファイル)が合流 | K5 |

## アプリは増える。エンジンは1つ(2026-08-12 発注者)

冒頭の「別のバイナリにする」は 2026-08-02 の2本(writer と calc)で始まり、
2026-08-04 に「新しいバイナリは作らない」と狭めた(docs/sekkei/python.ja.md)。
その後 pyoffice(2026-08-09)と builder(2026-08-11)が加わり、
**狭め方の形が違っていた**と分かった。2026-08-04 の狙いはバイナリの本数の
固定ではなく「**保守・検査・配布の対象を増やさない**」こと。増えて困るのは
**エンジン**であって、アプリではない。だからこう改める:

> **エンジンは1つ。アプリはいくつあってもよい — ただしアプリは薄いこと。**

薄いかどうかの物差しは1つ: **そのアプリにしか無い意味を持たないこと。**
「動いている表を Python から操る」は意味なのでアプリに置かない。
「GPUI でセルを描く」はアプリの仕事。アプリを足したくなったら問うのは
本数ではなく「**意味が漏れていないか**」。

いまのアプリは4つ — writer・calc(GPUI)、pyoffice(genoffice の Electron に
sidecar で載る)、builder(これから)。ブラウザー・スマホは WASM で後から
(「ネイティブが先」の順序のまま)。

**↑を改めた(2026-08-13 発注者)。builder は作らない。** builder が
やるはずだった「Web へ出す」は、**writer / calc の用紙の選択に「Web」を
1枚足すだけ**でよい — A4 や Letter と並ぶ**写し先の1つ**であって、
アプリではない。「壊さずに作り直せるサイトビルダー」の野心は畳む
(必要が実証されたら再訪)。アプリは3つ(writer・calc・pyoffice)。
これに伴い、builder を理由にしていた工事は理由を失う —
リボンの表の `tabs(app, lang)` 化(3つ組は来ない)、写し先の三重化の
備えなど。**builder と無関係に壊れている物**(条件付き書式の解決が
画面と紙の2箇所に写して書かれている件)だけが残る。

## 層は6つ(2026-08-12)

暗黙の「核 / アプリ」の2分では4つのアプリに足りなくなったので、名前を付ける:

| 層 | 問い | いまの実体 | 足りない物 |
|---|---|---|---|
| **模型** | 何であるか | kumihan(doc)・sheet・ooxml・lang | — |
| **見え** | どう見えるか | kumihan::layout(紙面)。**格子側は無い** | 表のセルの見えを決める1本(いまは calc の view.rs と paper/grid.rs が別々に解決している) |
| **写し先** | どこへ出すか | paper(PDF)、calc/writer の view.rs(画面) | Web / Flet 宣言(builder のとき。**paper と対の新クレート** — calc にも sheet にも入れない) |
| **操作** | 何ができるか | **無い**(意味が calc の中に閉じている) | 下の「操作の言葉を1本に」 |
| **つなぎ** | 誰が呼ぶか | pysheet(pyo3)・sidecar(genoffice)・calc の RPC | 薄いまま保つ |
| **アプリ** | どう触るか | ui + calc + writer(GPUI) | builder |

横断で lang(言葉)と生成スクリプト。**「操作」が「見え」の下ではなく横に
あるのが要点** — Python で `Range('A1').value = df` と書くとき、見えも
写し先も通らない。

なお名前の衝突(`sheet::Sheet` = xlsx のシート / `kumihan::Sheet` = 組み
上がった紙面)は改名しない — 両方を使う所では `as Grid` / `as Page` と
呼び分ける(paper と pysheet が既にそうしている)。

## 操作の言葉を1本に — calc をクレートに割る(2026-08-12 発注者)

出す側(画面も紙も同じ紙面を写す)は層になっているが、**「ブックに何が
できるか」の側に同じ言葉が無い**。Python から見た Book/Range の意味が
3箇所(calc/src/rpc.rs・pysheet・calc.py)に別々に書かれ、動詞が食い違う。
calc の undo はシート丸ごとの控えで、「誰が何を変えたか」を持てない —
builder の芯「壊さずに作り直せる」がこのままでは立たない。

**calc(約30,000行)を1クレートのままにしない。** クレート境界は Rust で
唯一の本物の壁で、「アプリは薄い」を気持ちではなく構造で守らせる。
`Cargo.toml` の依存に gpui が無いことは cargo が保証する。順番:

1. **段A**: UI でない塊を切り出す — `ops`(Book/Range の語彙。rpc.rs の
   9命令+calc/writer に写経されている13関数)と `pyrun`(Python 実行の
   機構 — Cage・埋め込み台本・plugins/UDF の管理。writer と共有になる)
2. **段B**(後で判断): アプリの中の部屋(view/cmds/picks)のクレート化。
   Calc 構造体の欄を API にする工事が要る
3. **その次**: `sheet::Edit` + `apply(&[Edit]) -> Undo` — undo を丸ごとの
   控えから Edit の列へ。これが builder の「どのセルが人の手か」と、
   差分再計算(いまは1セル直すたびにシート全体を回している)の両方への道

writer 側は表で1度通してから同じ問いを持ち込む(文書の編集は run 単位で
形が違う)。sidecar は載せ替えない — 向こうが決めた言葉を素通しするのが
役目で、翻訳の層を挟むと余計な欄が入る余地になる。

## 原則

- **核はUIに依存しない。** kumihan は紙面(置かれた文字の座標)だけを返す。
  画面も PDF も印刷も、同じ紙面を別の面に写すだけ — 画面と紙が一致する
- **互換は書式の境界で守る**(docx を読み書きする)。エンジンは継がない。
  vendor/ の web-apps・sdkjs・desktop-sdk・DesktopEditors は**挙動の教師**(参照資料)
- 検査できる単位で進める。里程標(K0〜K5)ごとに受入条件と自動テスト
- 難所を先に言う: **IME**(K2。カスタムUIでの日本語入力は歴史的な難所)、
  **OOXMLの広さ**(K3。全部は実装しない — 読めないものは読めないと言う)

## 蒸留して、上位互換を狙う(2026-08-12 発注者)

↑の「エンジンは継がない」を広げる — 撤回ではなく、**教師を増やす**ことと
**狙いを上に置く**こと。OSS の Office 互換エンジンから**蒸留**して、
それらの**上位互換**を狙う。

**蒸留するものは4つに分け、上の3つを取り、4つ目は取らない:**

| 取る | 具体 |
|---|---|
| **課題の並び** | 機能の一覧・ヘルプの目次(構成だけ借り、文章は写さない — docs/README.ja.md の蒸留の作法のまま) |
| **挙動** | 既定値・端の場合・式の意味(観察でよい) |
| **試験と実物** | 相手の試験一式・実物の資料。**免許を見る — 本体と別のことが多い** |
| ~~実装~~ | ソースコードは**写さない**(↑の原則のまま) |

**「上位互換」は2条件で測る**(言葉のままだと看板にしかならない):
①相手が通すものは全部通す(突き合わせ=同じ答えか、と、相手の試験=約束を
守るか、の**2本立てで、混ぜない** — pyoffice で 57,352 セル一致でも相手の
試験は 9件中7件落ちた)。②そのうえで、こちらにしか無いものがある
(日本語の組版・帳票・画面と紙の一致・Python 連携 — これも数える)。

**免許は設計の一部。** 製品は AGPL-3.0-or-later。MPL-2.0 / Apache-2.0 /
MIT / LGPL からは片道で取り込める。**GPL-2.0-only からはコードを1行も
取れない**(両立しない)— 挙動と試験の結果だけ。教師を1つ増やすときは、
**免許と「その相手から何を取るか」を先にここへ書いてから**手を付ける。

**相手は1つずつ**(調査の並列出しは 2026-08-07 に空回りした)。
0番は ONLYOFFICE(vendor 済・台帳 guide-tsukiawase-2 が既にこの機械)、
1番は LibreOffice(手元に 24.2.7.2、corpus_make.py が既に第2の書き手)。
関数はいますぐ数えられる — vendor の一覧 485 対 うちの 188(実装済みで
一覧に無い物が約30)。物差し tools/funcs_gap.py は**エンジン作成の車線に
渡した**(2026-08-12 発注者 — この車線は先に「操作の言葉を1本に」の段Aを
やる。共有の関数の置き場が無くて窓の修正を ui に押し込んだ、が合図)。


## 緑は「正しい」ではなく「この物差しでは差が出ない」(2026-08-10)

**1件なら事故、2件なら型**なので憲法に書く。同じ日に、別々のセッションが
**同じ形**で踏んだ。どちらも試験は緑で、しかも壊れていた。

| | 物差し | 見ていなかったもの | 結果 |
|---|---|---|---|
| docx の節 | PDF の**ページの大きさ**が正しいか | **字がその紙の中にあるか** | 向きの違うページで字が紙の外へ 87mm 出ていた |
| xlsx の突き合わせ | 両側の**セルの中身**が一致するか | 道具が「中身の無いセルは**両側で**飛ばす」と決めていた | **罫線だけのセル**が落ちているのを構造的に見られなかった |

**共通の型はこれ。**

> **両側に同じ簡略化を教えた物差しは、その簡略化の中にある差を永久に見ない。**
> 片側だけの誤りは見つかるが、**物差しが最初から見ないと決めた所**は、
> 何回まわしても緑のままになる。

だから緑を読むときは、**「正しい」ではなく「この物差しでは差が出なかった」**と
読む。合っているのは*測った物*だけで、測っていない物については何も言っていない。

**手当て**(どちらもそうやって直した):

1. **物差しをもう1本足す。** 節では「ページの大きさ」に加えて
   **字の座標が紙に収まっているか**を見る試験を別に立てた。
   xlsx では突き合わせとは別に**向こうの試験**(契約を見る道具)を通した
2. **突き合わせと試験を混ぜない。** 突き合わせは「**同じ答えか**」を見る道具、
   試験は「**約束を守るか**」を見る道具。前者が緑でも後者は落ちる
   (実際に 57,352 セル一致で 9件中7件落ちた)
3. **正規化・省略・飛ばしを入れたら、そこに札を立てる。** 「両側で飛ばす」は
   速さのための正しい判断でも、**そこが盲点になったと明記**しておく

**同じ日に4件出ている。** 上の2件のほかに、鍵割り当ての検査・文言の門番・
clippy と試験の**対象クレートの並び**(calc・writer・ui が門番の外に居る)が
同じ形だった — 数え上げは [HIKITSUGI.md](HIKITSUGI.md) にある。
そちらは「**検査はあるが、検査が見ている場所が本番とずれている**」という
言い方をしていて、こちらの「物差しが見ないと決めた所」と同じことを指している。
**4件なら型ではなく、この repo の癖**として扱う。

**実物で確かめる理由もここにある。** 自分で書いた文書は、自分の読み手が
読める形しか持っていない — 自分の物差しで自分を測る形になる。
数式(`xml:space` で LibreOffice の4つが全滅)も、節(1ページ目だけ紙が違う)も、
**実物を通して初めて出た**。手で組んだ試験は全部緑だった。


## 設計の部屋割り(2026-08-08 発注者「一つのファイルで管理するのは無理」)

1751 行に育ったので、calc/src の部屋割りと同じ作法(**純移動・文言不変**)で
テーマ別の部屋に割った。ここ(SEKKEI.md)に残るのは憲法 — 上の各節と、
この目次と、書き込みの規則だけ。

| 部屋 | 中身 |
|---|---|
| [docs/sekkei/calc.ja.md](docs/sekkei/calc.ja.md) | calc: 5つの直し・本家ガイド突き合わせ(台帳の使い方)・calc/src の部屋割り |
| [docs/sekkei/writer.ja.md](docs/sekkei/writer.ja.md) | writer: 編集モデル(run)・挿入系・変更履歴・HTML・灰色ゼロ・保護 |
| [docs/sekkei/python.ja.md](docs/sekkei/python.ja.md) | Python: ブックはコードを運ばない(2026-08-09)・plugins と UDF・officework 連携(部屋の中に経緯ごと残る) |
| [docs/sekkei/nihongo.ja.md](docs/sekkei/nihongo.ja.md) | 日本語: なぜ作るか・校正・辞書・K4(縦書き/ルビ/均等割付)・右横書き |
| [docs/sekkei/sugata.ja.md](docs/sekkei/sugata.ja.md) | 姿(UI): リボン・額縁・テーマ色・設定・罫線の直交モデル |
| [docs/sekkei/ayumi.ja.md](docs/sekkei/ayumi.ja.md) | 歩みと商い: 目標・里程標・名前と免許・製品名・配布・e-shop・同類との距離 |
| [docs/sekkei/pyoffice.ja.md](docs/sekkei/pyoffice.ja.md) | pyoffice: genoffice に officework エンジンを載せる(読み・計算まで。書きはやらない) |
| [docs/sekkei/builder.ja.md](docs/sekkei/builder.ja.md) | builder(4本目): セルの模型で Web/アプリ — **作らないと改めた(2026-08-13)。Web は用紙の選択へ** |

**書き込みの規則**(従来の習慣の明文化):

1. 決定と踏んだ穴は**その日のうちに部屋へ**書く(コミットと同じ回で)。
   どの部屋か迷う小さい物はいちばん近い部屋の末尾へ — 育ったら節に切り出す
2. 節の頭に**日付と誰の決定か**(発注者確定 / 発注者報告 / 当方の見立て)
3. 決定を覆すときは古い節を消さず「↑を狭めた/改めた(日付)」で上書きの
   節を足す — 経緯ごと残す(Python の節がその型)
4. 探すときは `grep -rn "語" SEKKEI.md docs/sekkei/` — 正本はここと部屋で全部
   (枚数は書かない — 部屋は増える。実際 7枚と書いた2日後に pyoffice と
   builder が増えた)
5. 実装の在庫(何ができていないか)は台帳
   [docs/guide-tsukiawase-2.ja.md](docs/guide-tsukiawase-2.ja.md)、
   使い方は**マニュアル**(docs/*-manual.ja.md — 管理の規則は
   [docs/README.ja.md](docs/README.ja.md))。
   設計(なぜ)・在庫(どこまで)・使い方(どうやる)の三帳簿を混ぜない
6. **方針を廃止する回で、`tools/sunset_check.py` の表にも語を足す。**
   使い方の帳簿(README・マニュアル)は「できることだけ書く」ので、
   廃止語が現在形で残っていたら CI が落ちる。設計の部屋は対象外(経緯を
   残す場所)。この検査は表に載せた語しか見ない — 載せ忘れた廃止は永久に
   緑で通るから、この規則で塞ぐ(README の「ブックに載せられます」が
   9日間残った 2026-08-12 の反省)
