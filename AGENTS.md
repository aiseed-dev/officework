# officework で作業する AI への決まり

この文書は、Claude・Codex などの AI がこのリポジトリで作業するときの共通の決まりです。
Claude は CLAUDE.md からこの文書を読み込みます。

先に読む文書:

- `docs/dev/README.ja.adoc`:開発者マニュアル。crate の地図と、コードを書くときの決まり
- `SEKKEI.adoc` と `docs/sekkei/`:決めたことと、その経緯
- `HIKITSUGI.adoc`:引き継ぎ。この Mac の環境と直近の状態

## 言葉づかい

2026-08-08 から4回指摘されています。**普通の言葉で書きます。**

発注者の言葉: 「日本のプログラマーは絶滅するから真似をしないで」(2026-08-17)

### 過去に指摘された言葉

| 使わない | 使う |
|---|---|
| 台本 | スクリプト |
| 門番 | チェック、検査 |
| 便(作業の1回分の意味) | 回、作業 |
| 的(ターゲットの意味) | 対象、対象 OS |
| 物差し | 基準、確認方法 |
| 畳む | 閉じる、削除する |
| 素の | 普通の、元の |
| 口(API・ソケットの意味) | API、ソケット、入り口 |
| 橋(プロセス間の接続の意味) | 接続、〜を操る仕組み |
| 皮 | 互換層、ラッパー |
| 正本 / 副 | 元、原本 / 訳 |
| 束縛 | 変数 |
| 語彙 | 書き方 |
| 着せる | 使う |
| 実測 | 実際に動かして確かめた |
| 組み手 / 折り手 | レイアウト処理 |
| 巻物 | 連続表示 |
| 名乗り | 宣言 |
| 釦 / 板 / 檻 / 錨 / 糸 | ボタン / パネル / サンドボックス / アンカー / スレッド |
| 生成器 / 家訓 | 生成スクリプト / 方針 |
| 殻(アプリの意味) | アプリ、本体 |
| 耳(タブの意味) | タブ、シート見出し |
| 升 / 升目 | セル |
| 泵 | (使わない。普通の説明文で書く) |
| 面(画面の意味) | 画面、編集画面 |
| 枠 / 器(仕組みの意味) | 仕組み、一覧 |
| 鍵(設定やキーボードの意味) | キー(暗号の鍵・公開鍵はそのまま) |
| 札(ボタンやタブの文字の意味) | ラベル、表示名 |
| かぶり / 重なり | 重複 |
| 持ち場 | 受け持ち、担当、「〜が処理する」 |
| 裏方 / 裏で呼ぶ | 内部の処理、「〜が処理する」 |
| 車線 | 作業の流れ、別の作業 |
| 踏み跡 | 分かったこと、経緯 |
| 腰(ベースラインの意味) | ベースライン |
| 箱(行の高さの意味) | 行の高さ、行の枠 |

表に無い言葉でも、**「普通そう言うか」で判断します**。迷ったら Excel や
Word のヘルプが使う言葉を選びます。

### 書き方

やめること:

- 造語や比喩で言い換える
- 格言のように言い切る(「〜は〜より悪い」「〜が筋」)
- 太字を並べる(1段落に多くて1つ)
- 体言止めやダッシュ(—)でつないで、主語と述語をあいまいにする
- 1つの文に複数のことを詰める
- 発注者の主張を和らげるために、根拠の無い文を足す

書くこと:

- 主語と述語をそろえた普通の説明文
- 1つの文には1つのこと
- 専門用語は一般に通じるカタカナ語
- 説明は「です・ます」

これは文書だけでなく、**画面の文言・発注者への返答**にも当てはまります。
書いた後に「普通の人が読んで分かるか」を1度見直します。

- **コミットメッセージは英語で書きます**(2026-08-30 発注者)。上の言葉の決まりは
  日本語の文のためのもので、コミットには当てはまりません。英語でも「1文1事」
  「言い切らない」「太字を並べない」は同じです。
- **コードの中のコメントも英語で書きます**(2026-09-18 発注者)。新しく書く
  コメントは英語で書き、触った所の日本語のコメントは同じ回で英語にします。
  発注者の言葉の引用は残さず、決めた内容だけを英語で書きます(日付は残してよい)。
- `python3 tools/kotoba_list.py` は、利用者が読む文書と画面の文言から、Excel と
  Word の用語と役所の docx の本文に無い言葉を多い順に出します。合否は出しません。
  出た言葉は言い換えるか、普通の言葉なら `tools/kotoba_tsukau_tsuika.txt` に足します。

### 利用者向けの文書は、さらに厳しく

1. **Excel / Word の公式用語で書きます。** 利用者はヘルプで覚えた言葉で探します。
   式 → 数式、真偽 → 論理値、様式(docx)→ スタイル、入力規則 → データの入力規則
2. **内輪の日付や決定の注記を入れません。** 「(2026-08-09 発注者確定)」の類は
   設計の文書に書きます。

マニュアルを書くときの決まりは `.claude/skills/manual/SKILL.md` にあります。

## 作業の進め方

- **push は発注者がします。** コミットまでで止めます。
- **ワークツリーは使いません。** `~/dev/officework` で直接作業します。
- コミットは**自分が触ったファイルだけ**を名前で指定して1回で行います
  (`git add <paths> && git commit -m "…" -- <paths>`)。git の index は他の
  セッションと共有しているためです。`git stash` も使いません。
- 秘密の値(API キーなど)は画面に出しません。あるかどうかだけ確かめます。
- 頼まれた分だけ変えます。1 文を頼まれたら 1 文を直し、説明の文を勝手に足しません。
- ダウンロード(pip install を含む)は、名前・出所・大きさを言って許可を得てからします。

## 目標と判断の基準

- 第一の目標は、docx と xlsx を読み込んで、Word・Excel と同じに表示・印刷することです。
  新しい機能より先に、この一致を上げます。
- 画面(リボン・ダイアログ・右パネル)は Euro-Office に合わせます。Word・Excel の
  並びや名前には合わせません。
- **Word・Excel との違いは、直す前に OOXML(ECMA-376)で確かめます。** 要素・属性・
  値と規約の項の番号を確かめ、推測や勝手な数字で直しません。手順は
  `.claude/skills/ooxml-check/SKILL.md` にあります。
- **何でも Office の真似をしません**(2026-09-23 発注者)。違いは OOXML でどちらが
  正しいかを決め、こちらが正しい所は残します。
- 全件の突き合わせを毎回はしません。直した文書を Word・Excel と行ごとに比べます。
  報告に全件の数を出しません。
- 依存の版は固定して逃げず、月に1回上げて、動かして直します。

## Word と Excel を使うとき

- この Mac には Word と Excel があります。開くのは発注者の許可を得てからにします。
- 開かせた文書はその都度閉じます。閉じられないときは、そこで止まって発注者に言います。
- 発注者が開いている Excel のブックや窓を、閉じたり終了させたりしません。

## 組み立てと検査(この Mac)

Python は `.venv` を使います。2026-09-24 からは conda で作った Python 3.14 の環境で、
libpython は `.venv/lib` にあります。システムの python3 は 3.9 で、使えない場面があります。

```bash
# workspace 全体のコンパイル(エンジンの共有の型を触ったら毎回)
PYO3_PYTHON=$PWD/.venv/bin/python cargo check -q --workspace --all-targets

# 触った crate のテスト
PYO3_PYTHON=$PWD/.venv/bin/python cargo test -q -p book -p sheet

# pysheet(Python のテストも走る)
PATH=$PWD/.venv/bin:$PATH DYLD_FALLBACK_LIBRARY_PATH=$PWD/.venv/lib \
  PYO3_PYTHON=$PWD/.venv/bin/python cargo test -q -p pysheet

# アプリ(target/release/officework)。LTO のため 10〜25 分かかります
PYO3_PYTHON=$PWD/.venv/bin/python cargo build -q --release --bin officework
```

- `.venv` の `officework` は、手元のコードから入れます(PyPI の版ではありません)。
  エンジンを直した後も、同じコマンドで入れ直します。
  ```bash
  cd pysheet && CONDA_PREFIX=$PWD/../.venv PYO3_PYTHON=$PWD/../.venv/bin/python \
    ../.venv/bin/maturin develop --release
  ```
- `.venv` を作り直して Python の版が変わったときは、PyO3 の組み立て済みの物を
  消してから組みます。消さないと、前の Python の libpython につながったままになり、
  テストが `Library not loaded: @rpath/libpython3.13.dylib` で落ちます。
  ```bash
  cargo clean -p pyo3-build-config -p pyo3-ffi -p pyo3 -p pyo3-macros-backend -p pysheet
  ```
- 全部のテストは回しません。触った crate のテストと、workspace の check を回します。
- calc のテストのうち 5 件は、`.venv` に matplotlib と scipy が無いと失敗します
  (`equation_tests`・`solver_tests`・`recalc_tests` の一部)。環境の問題なので、
  報告ではそう書きます。
- 紙面の回帰テスト(`paper/tests/kaiki.rs`)が落ちたら、`target/kaiki/` の絵を見て、
  正しいと確かめてから `KAIKI=とりなおす cargo test -p paper --features e --test kaiki`
  で取り直します。
- 生成物は手で書きません。
  - 画面の文言:`ui/i18n/<言語>.json`(15 言語)に書いて
    `python3 ui/gen_lang.py <言語…>` と `python3 ui/gen_lang.py --check`
  - 関数の一覧と説明:`calc/gen_funcs.py` の `HAND_JA` と `calc/funcs_hand.json` に書いて
    `.venv/bin/python calc/gen_funcs.py --all`(3.9 では動きません)
- ビルド先は `target/` だけを使います。`CARGO_TARGET_DIR` を別の場所に作りません
  (2026-09-22、合わせて約 490GB になっていました)。
- `flatpak-builder` は `--delete-build-dirs` を付けて実行します。終わったら
  `packaging/flatpak/.flatpak-builder` と `build-dir` を消します(2026-09-22、約 160GB)。
- ビルドの終わりを待つとき、`until ! pgrep -f 'cargo build'` のようなループを
  書きません。ループを動かしているシェルのコマンド行にも同じ文字があるので、
  自分自身に一致して終わりません。

## 画面を直したら

**実際に動かして見てから報告します。** テストが通っただけでは報告しません。
手順は `.claude/skills/app-check/SKILL.md` にあります。要点:

- 発注者の settings.toml を書き換えないように、作業用の HOME で起動します。
- Mac では外からクリックやキーを送れません。アプリの API(ソケット)で操作し、
  窓を `screencapture -l` で撮って見ます。
- 自分が起動したアプリだけを終わらせます。発注者の窓を巻き添えにしません。
- キー入力のように確かめられなかったことは、報告にそう書きます。
