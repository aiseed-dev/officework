# **配る物の門。** push したら回ります。
#
# 見るのは1つだけです。**いま配っている物が壊れていないか。**
# いま配っているのはエンジン(`pip install officework` で入る分)です。
# アプリはまだ配っていません。
#
# 2026-09-03 発注者「単体検査、将来の検査は、ここからは外す」。それまで
# ここは単体の試験も clippy もスマホの組み立てもアプリの組み立ても
# 抱えていて、**配る人が誰も触れない所の赤で公開が止まっていました**。
# 分け方は docs/release-tejun.ja.adoc の「検査は2つに分けます」にあります。
#
# 仕事は3つです:
#
#   match          配る物と、配る物について書いた文書が食い違っていないか。
#                  組み立てのいらない突き合わせなので速い
#   core-windows   配る wheel の3つ目の OS で pysheet が組めるか
#   python         **配る形で確かめる。** 手元の木ではなく、素の環境に
#                  wheel を入れて、手引きの見本がそのまま動くか
#
# 単体の試験と clippy は tests.yml(普段の検査)にあります。
# スマホ(Android / iOS)の wheel は wheels-mobile.yml です。
#
# **突き合わせをここに残す理由。** 2026-08-10 に、配っている import 名
# (officework.sheet)と文書の見本(office_sheet)が食い違っていたのを
# 人手で見つけました。文書は配り物の一部なので、ここで見ます。

name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# 同じ枝で走っているものは畳む(押し直したら前のは要らない)
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # 配る物と、配る物について書いた文書が食い違っていないか
  match:
    name: 配る物と文書が揃っているか
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          # **失敗した回でも組んだ物を残す。** 既定では job が失敗すると
          # 保存しないので、組み立ての後ろの段で落ちるたびに、次の回も
          # 一から組み直しになる(2026-08-17、Windows が 12分14秒かけて
          # 組んだ後、その次の1行で落ちて結果が捨てられた)
          cache-on-failure: "true"
      # **単体の試験と clippy はここにありません。** tests.yml(普段の検査)へ
      # 移しました(2026-09-03 発注者「単体検査、将来の検査は、ここからは
      # 外す」)。この仕事が見るのは、配る物と、配る物について書いた文書が
      # 食い違っていないかです。
      - name: サイドカーが組めるか(pyoffice)
        run: cargo build -p sidecar
      # **スマホ(Android / iOS)の組み立ては、ここでは見ません。**
      #
      # まだ配っていないからです(2026-09-03 発注者「将来の検査は、ここからは
      # 外す」)。配ると決めたときに戻します。
      #
      # 落ちていた所を書き残します。数式(typst)が `stacker` と `psm` を
      # 連れてきました。この2つは C のコードを対象 OS 向けに翻訳するので、
      # C の道具が要ります。この段は Linux の runner に翻訳器だけを入れて
      # 安く見る所だったので、`kumihan` を引く `sidecar` はここでは組めません
      # (2026-09-03 に手元で同じ命令を回して確かめました)。
      # `face` と `lang` は `kumihan` を引かないので、そのまま組めます。
      #
      # C の道具のある機械は wheels-mobile.yml です(Android は NDK の clang、
      # iOS は macOS の runner)。**そちらで組めるかどうかは、まだ確かめて
      # いません。** 数式が入ったのはこの版からです。
      #
      # pysheet も同じ理由でここでは見られません(2026-08-29 に polars が
      # 同じ2つを連れてきました)。Windows は下の core-windows の仕事で見ます。
      - name: 設定の置き場の名前が実物と揃っているか
        # **利用者が最初に読む所が、いちばん古かった。** 2026-08-16 に
        # 置き場の名前を office から officework へ移したのに、リリースの
        # 言い分・.deb の説明・乗り換えの手引きが古い名前のままで、しかも
        # UDF の置き場は plugins から funcs に分けた後だった —
        # **言われたとおりに置いても動かない**案内を配るところだった
        run: python3 tools/config_dir_check.py
      - name: 古めかしい言い方が文書に残っていないか
        # 2026-08-08 から4回、同じ指摘を受けている(「日本のプログラマーは
        # 絶滅するから真似をしないで」2026-08-17)。禁止する言葉の表を作っても
        # 書くたびに新しい比喩を思いつくので、機械に見てもらう。
        # 言い換えの表は CLAUDE.md にある
        run: python3 tools/kotoba_check.py
      - name: 廃止した方針が現在形で文書に残っていないか
        # 2026-08-09 に「ブックはコードを運ばない」が確定したのに、README は
        # 9日間「載せて持ち運べます」と言い続けていた(2026-08-12 発見)。
        # マニュアルは同じ回で直っていた — README だけ思い出し頼みだった。
        # 対象は使い方の帳簿(README・マニュアル)だけ。設計(SEKKEI と
        # docs/sekkei/)は経緯を残す場所なので見ない。
        # **限界**: 表に載せた語しか見ない。廃止を決めた回で表にも足すこと
        # (SEKKEI 書き込みの規則6)
        run: python3 tools/sunset_check.py
      - name: 対応表が実物と揃っているか(メニュー・API・本家)
        # **同じ操作を4つの言い方で引けるようにする表**(2026-08-24 発注者
        # 「メニュー、API、python-docx、openpyxl の関係をまとめた一覧表を作れ」)。
        # 画面のボタン・officework・python-docx / openpyxl が1行に並ぶ。
        # ボタンか API が増えた回に、この表だけ古くなるのを止める。
        # 落ちたら --write で書き直してコミット
        run: python3 tools/api_taiou.py
      - name: まとまりの検査が効いているか(対応表)
        # **同じことをするボタンは1行にまとめる**(2026-08-30 発注者
        # 「こういうのは、ボタングループとして処理すべきでは」)。まとまりは
        # 手で宣言するので、リボンが変われば黙って古びる。3つの崩し方で
        # 検査が落ちることを確かめ、効いていない検査を通さない
        run: python3 tools/api_taiou.py --self-test
      # **アプリについての突き合わせは、ここにありません。**
      # リボンの表・13言語の訳・キーの一覧・コマンドの手引き・ダイアログの
      # 値・画面の文言は、tests.yml の「アプリと文書が揃っているか」へ
      # 移しました。アプリをまだ配っていないからです(2026-09-03)。
      # AsciiDoc の書き方の表と `face` の依存も、同じ理由で向こうです。
      - name: 利用者向けの文書に内輪の注記が無いか
        # **書いている本人には内輪に見えません。** 決めた経緯を覚えている
        # うちは、その注記が親切に見えます。使う人は経緯を知らないので、
        # 読む邪魔にしかなりません(2026-08-24 発注者「こんな文章をかくな。
        # これは、ユーザーマニュアルの一部」— 対応表の頭に日付・決めた人・
        # 生成の仕組み・作業の残りを書いていました)。
        # 見るのは使い方の帳簿だけで、設計と台帳は経緯を残す場所なので見ない
        run: python3 tools/manual_uchiwa_check.py
  # **アプリ(calc / writer)の仕事はここにありません。**
  #
  # まだ配っていないからです(2026-09-03 発注者「将来の検査は、ここからは
  # 外す」)。GPUI を組むので一番時間の掛かる仕事でもありました。配ると
  # 決めたときに戻します。戻すときは 3 OS(Linux / macOS / Windows)で、
  # Linux は GPUI が繋ぐ物(libxkbcommon・libxcb・libfontconfig1-dev・
  # cmake・clang)を apt で入れる必要があります。
  #
  # **配る wheel の3つ目の OS。** Windows でも組めることを push のたびに
  # 見ます。タグを押してから知るのでは遅いためです。
  # Windows での単体の試験は tests.yml にあります。
  core-windows:
    name: pysheet が Windows で組めるか
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          # **失敗した回でも組んだ物を残す。** 既定では job が失敗すると
          # 保存しないので、組み立ての後ろの段で落ちるたびに、次の回も
          # 一から組み直しになる(2026-08-17、Windows が 12分14秒かけて
          # 組んだ後、その次の1行で落ちて結果が捨てられた)
          cache-on-failure: "true"
      - name: pysheet が組めるか(配る wheel の3つ目の OS)
        # v0.2.0 のタグで初めて発覚: pysheet が ops に依存した日に、ops の
        # unix 専用 API(sock_path の uid・/dev/urandom・0600)が Windows の
        # wheel を壊しました。push のたびに見ます — タグを押してから知るのは
        # 遅いためです。
        #
        # 前は Linux から `--target x86_64-pc-windows-msvc` で見ていました。
        # polars が C のコード(psm・stacker)を連れてきた 2026-08-29 から、
        # それでは windows.h が無くて止まります。**本物の MSVC がある
        # ここへ移しました**(2026-08-30)
        run: cargo check -p pysheet

  # 配る形で確かめる。**手元の木ではなく wheel を入れた素の環境で**
  python:
    name: wheel を入れて見本どおりに動くか
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          # **失敗した回でも組んだ物を残す。** 既定では job が失敗すると
          # 保存しないので、組み立ての後ろの段で落ちるたびに、次の回も
          # 一から組み直しになる(2026-08-17、Windows が 12分14秒かけて
          # 組んだ後、その次の1行で落ちて結果が捨てられた)
          cache-on-failure: "true"
      - name: wheel を組む
        run: |
          pip install maturin
          maturin build -m pysheet/Cargo.toml --release --out dist
      - name: 配ってはいけない物が入っていないか
        # **`.gitignore` に書いてあるだけでは止まらない。** maturin は
        # python-source の側を見ないので、組む機械に `.pyc` があれば同梱される。
        # CI は checkout 直後で `.pyc` が無いから緑になる — つまり**この検査は
        # 手元の事故を止めない**。止めるのは pyproject の exclude で、これは
        # その exclude が外れたときに気付くための見張り(2026-08-10)
        run: |
          if python3 -m zipfile -l dist/*.whl | grep -q __pycache__; then
            echo "::error::wheel に __pycache__ が入っています(pyproject の exclude を確かめてください)"
            python3 -m zipfile -l dist/*.whl | grep __pycache__
            exit 1
          fi
      - name: 素の環境へ入れる
        run: |
          python -m venv /tmp/v
          /tmp/v/bin/pip install dist/*.whl
      - name: 文書の見本がそのまま動くか
        # **README と手引きに載っている書き方をそのまま打つ。**
        # ここを手元の木で試すと、ソースの officework/ を掴んでしまい
        # 配った物を確かめたことにならない(2026-08-09 に踏んだ)
        env:
          BOOK: ${{ github.workspace }}/sample/見積書.xlsx
        run: |
          cd /tmp
          /tmp/v/bin/python - <<'PY'
          import os
          from officework import sheet

          b = sheet.Book.open(os.environ["BOOK"])
          assert b.sheet_names, "シートが読めていない"
          assert b.unsupported == [], f"読めなかった部品がある: {b.unsupported}"
          s = b[b.sheet_names[0]]
          s["A30"] = "サンプル商事株式会社"
          b.save("/tmp/out.xlsx")

          again = sheet.Book.open("/tmp/out.xlsx")
          assert again[again.sheet_names[0]]["A30"].value == "サンプル商事株式会社"
          print("見本どおりに動いた:", b.sheet_names)
          PY
      - name: docx の見本もそのまま動くか
        # **README と手引きに載っている docx の書き方をそのまま打つ。**
        # 2026-08-10 の 0.1.2 で、見本が動かないまま配っていたのを踏んだので、
        # エンジンが増えるたびにここへ足す(sheet だけ緑でも足りない)
        env:
          DOC: ${{ github.workspace }}/sample/報告書.docx
        run: |
          cd /tmp
          /tmp/v/bin/python - <<'PY'
          import os
          from officework import doc

          d = doc.Doc.open(os.environ["DOC"])
          assert len(d) > 0, "段落が読めていない"
          assert d.unsupported == [], f"読めなかった部品がある: {d.unsupported}"
          assert d.tables, "表が読めていない"
          before = d.tables[0].values()

          n = d.replace("業務", "作業")
          assert n > 0, "置き換えが1件も起きない"
          d[0].text = "差し替えた見出し"
          d.save("/tmp/out.docx")

          again = doc.Doc.open("/tmp/out.docx")
          assert again[0].text == "差し替えた見出し", "差し込んだ字が保存されない"
          assert again.tables[0].values() == before, "保存で表が変わった"
          # **原本の部品が減っていないこと** — 売り文句の土台
          import zipfile
          with zipfile.ZipFile(os.environ["DOC"]) as z:
              a = set(z.namelist())
          with zipfile.ZipFile("/tmp/out.docx") as z:
              b = set(z.namelist())
          assert not (a - b), f"保存で原本の部品が消えた: {sorted(a - b)}"
          print("docx の見本どおりに動いた: 段落", len(again), "表", len(again.tables))
          PY
      - name: 名前つき記入欄の見本もそのまま動くか
        # README の d.fill("宛先", …) の行を配った wheel で打つ。sample に
        # 記入欄つきの docx が無いので、最小の w:sdt(本文と表の中に1つずつ)を
        # その場で作って fields / fill / extract を通す(2026-08-13 に追加)
        run: |
          cd /tmp
          /tmp/v/bin/python - <<'PY'
          import zipfile
          doc_xml = '''<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
          <w:document xmlns:w="http://schemas.openxmlformats.org/wordprocessingml/2006/main">
          <w:body>
          <w:p><w:sdt><w:sdtPr><w:tag w:val="宛先"/><w:text/></w:sdtPr>
          <w:sdtContent><w:r><w:rPr><w:b/></w:rPr><w:t>ここに社名</w:t></w:r></w:sdtContent></w:sdt></w:p>
          <w:tbl><w:tblGrid><w:gridCol w:w="4000"/></w:tblGrid>
          <w:tr><w:tc><w:p><w:sdt><w:sdtPr><w:tag w:val="金額"/><w:text/></w:sdtPr>
          <w:sdtContent><w:r><w:t>0</w:t></w:r></w:sdtContent></w:sdt></w:p></w:tc></w:tr></w:tbl>
          </w:body></w:document>'''
          ct = '''<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
          <Types xmlns="http://schemas.openxmlformats.org/package/2006/content-types">
          <Default Extension="rels" ContentType="application/vnd.openxmlformats-package.relationships+xml"/>
          <Default Extension="xml" ContentType="application/vnd.openxmlformats-officedocument.wordprocessingml.document.main+xml"/>
          </Types>'''
          rels = '''<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
          <Relationships xmlns="http://schemas.openxmlformats.org/package/2006/relationships">
          <Relationship Id="rId1" Type="http://schemas.openxmlformats.org/officeDocument/2006/relationships/officeDocument" Target="word/document.xml"/>
          </Relationships>'''
          with zipfile.ZipFile("/tmp/fields.docx", "w") as z:
              z.writestr("[Content_Types].xml", ct)
              z.writestr("_rels/.rels", rels)
              z.writestr("word/document.xml", doc_xml)

          from officework import doc
          d = doc.Doc.open("/tmp/fields.docx")
          assert d.fields() == [("宛先", "ここに社名"), ("金額", "0")], d.fields()
          assert d.fill("宛先", "サンプル商事株式会社") == 1
          # **無い欄は KeyError で止まります。** 原稿の手引きの「その名前の
          # 記入欄が無ければ、止まって知らせます」に合わせた形です
          try:
              d.fill("無い名前", "x")
              raise AssertionError("無い欄で黙って成功してはいけない")
          except KeyError:
              pass
          assert d.extract("宛先") == "サンプル商事株式会社"
          assert d.extract("無い名前") is None
          d.save("/tmp/fields-out.docx")
          again = doc.Doc.open("/tmp/fields-out.docx")
          assert again.fields() == [("宛先", "サンプル商事株式会社"), ("金額", "0")], "保存で欄が壊れた"
          print("記入欄の見本どおりに動いた:", again.fields())
          PY

      - name: 存在しない名前が文書に残っていないか
        # office_sheet は 1cae2f0 で officework.sheet へ移した。**貼ると
        # ImportError になる名前が文書に残っていたら落とす**
        # 2026-08-10 に officework.doc を足した。**存在しない名前を増やさない** —
        # office_doc も officework.docx も import できない。
        # 見張るのは「動きそうに見えて動かない名前」だけ。officework.writer は
        # 設計文書が「今後こう増やす」と書いている先の話なので入れない
        run: |
          bad=0
          for name in office_sheet office_doc "officework\.docx"; do
            if grep -rn "$name" --include="*.md" . ; then
              echo "::error::$name は存在しない import 名です(officework.sheet / officework.doc へ)"
              bad=1
            fi
          done
          exit $bad
