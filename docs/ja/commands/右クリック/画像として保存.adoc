= 画像として保存(SVG)

図形を右クリックすると出ます。

*状態: 実装済み* — いま使えます

== 何をするか

選んでいる図形を SVG のファイルに書き出します。

*複数の図形を選んでいれば、まとめて1枚にします。*
SmartArt は図形の集まりとして組んであるので、1つだけ書き出すと図の一部しか残りません。

== ダイアログ

保存先を選ぶ窓が出ます。ファイル名の既定は `figure.svg` です。
取りやめると、その旨を状態行に出します。

== プログラムから

(まだありません)
