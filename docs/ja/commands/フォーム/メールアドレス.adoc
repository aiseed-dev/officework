= メールアドレス

フォームにあります。

*状態: 実装済み* — いま使えます

== 何をするか

メールアドレスを書く記入欄を置きます。

中身は字の欄と同じですが、*何を書く欄かの印*が付きます。
docx では `jo:email` という印で往復するので、開き直しても種類が残ります。

**打った字が正しいかどうかは見ていません。** 印は仕分けのためです。

== ダイアログ

(ダイアログは出ません)

== プログラムから

[source,python]
----
mcp.doc_fill({名前: 値})
----

