= 予測シート

データにあります。

*状態: 実装済み* — いま使えます

== 何をするか

並んだ数から、この先の値を予測します。
予測は指数平滑法(Holt-Winters)で行い、周期があれば自動で見つけて使います。
結果は新しいシートに、元の値と予測値の折れ線グラフつきで入ります。

Python に numpy と scipy が要ります。無いときは、状態行にそう出ます。

== ダイアログ

何期先まで予測するかを聞きます。
数を打って Enter を押します。空のまま Enter を押すと 6 期になります。

== プログラムから

(まだありません)
