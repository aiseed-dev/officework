= 外部リンク(値で取り込む)

データにあります。

*状態: 実装済み* — いま使えます

== 何をするか

他のブックの値を取り込みます。

*取り込むのは値だけです。参照は張りません。*
元のブックを動かしたり消したりしても、この表は壊れません。
その代わり、元が変わっても自動では追いません。取り込み直してください。

== ダイアログ

xlsx を選ぶ窓が出ます。

== プログラムから

(まだありません)
