= チェックボックス

挿入にあります。

*状態: 実装済み* — いま使えます

== 何をするか

選んだ範囲の空のセルを、チェックボックスにします。

セルの値は TRUE と FALSE です。空白キーで切り替わります。
式から `=E4` や `=COUNTIF(E:E,TRUE)` として普通に使えます。

*中身のあるセルは潰しません。* 空のセルにだけ置きます。
既に TRUE / FALSE が入っているセルは、そのままチェックボックスとして見えます。

== ダイアログ

(ダイアログは出ません)

== プログラムから

(まだありません)
