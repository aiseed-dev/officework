= 複数レベルのリスト

ホームにあります。

*状態: 実装済み* — いま使えます

== 何をするか

段の深さごとに違う番号の付け方をします。

== ダイアログ

(ダイアログは出ません)

箇条書きにして1段深くします。段は `Tab` と `Shift+Tab` でも動かせます。
印は段で変わります(`・` → `○` → `■`)。

== プログラムから

(まだありません)

