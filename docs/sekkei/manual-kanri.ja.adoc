= マニュアルの管理(作る側の資料)

利用者向けの一覧は link:../README.ja.adoc[docs/README.ja.adoc] です。
そちらには管理の話を書きません(2026-08-26 発注者「ユーザーがみるものだから
簡潔に。ただし、利用するために必要なことはすべて書け」)。

== 管理の対象

当面、管理するのは**利用者向けの冊子の日本語と英語の対**だけです
(2026-08-26 発注者。三帳簿の見取り図は取り下げた)。
英語版が正本(primary)、日本語版が副 — 各冊子の頭にそう書きます。

英語版がまだ無い冊子と作る順:
docx-xlsx-tono-chigai → api-taiou → commands/(169枚。Opus の流し込みが
済んでから)。

設計(SEKKEI・sekkei/)と在庫の台帳(guide-tsukiawase-2)は作る側の資料で、
この管理の外です。docs/ に残っている corpus・engine・pysheet-gokan・
mac-signing も作る側の物ですが、**場所は動かしません** — PyPI の README など
公開済みのリンクが刺さっているためです。

articles/ は公開用の書き物で、この規則は掛かりません(その時点の事実と
見立てを残す物。挙動が変わっても遡って直さない。発注者の一人称で書く)。

== 管理の規則

. *嘘は欠落より悪い。* 挙動を変えるコミットは、**同じ回で**マニュアルの該当箇所も直す
. **新機能の追記は遅れてよい**が、遅れは下の「追随待ち」に一行で控える。黙って古いままにしない
. *できることだけ書く*(リボンの灰色と同じ方針)。「〜できます(予定)」は書かない。
**ただし `commands/` は別**です — こちらは*手引きが先で、実物が後*。
画面の灰色のボタンと同じ考え方で、*あることは見せて、状態で言います*。
書く状態は3つ: *実装済み*(いま使えます)・*未実装*(まだ使えません)・
*廃止予定*(なくす予定です。行き先を書く)
. 両言語とも直すのが原則。急ぐときは正本(英語)を先に直し、日本語は追随待ちに載せる — どちらか一方だけが直っている状態を規則の外に置かない
. 実測で書く(python-manual の流儀 — 「全部この機械で実測してある」を守る)
. *生成した節は、手で直さない。* 手引きの中には道具が起こす節がある
(キーの一覧・すべての操作・対応表)。**手元で検査を回せば、ずれていても
自分で直ります** — 直った分をコミットに入れる。CI では直さずに落とすので、
入れ忘れはそこで分かる
. *蒸留の作法*(2026-08-08 発注者「Excel のマニュアルを蒸留して」):本家(Excel)のヘルプは**課題の並びの教師** — 使う人が知っている仕事の一覧として構成だけ借り、*文章は写さない*(vendor を挙動の教師にした台帳と同じ流儀)。成果物は from-excel(課題→ここでの道/やらない理由)

== 追随待ち(直したら消す)

* calc-manual(ja/en): 2026-08-07〜08 の突き合わせ分の総ざらい —条件付き書式の第2版(バー・スケール・アイコン)、ピボットの絞り込みとグループ化、スパークライン3種、テキスト取り込みウィザード、R1C1、反復計算などが「挿入」「データ」節にまだ薄い(図形と @コマンドは2026-08-08 に追随済み)
* from-excel: 第3稿+英語版(2026-08-08 — 本家ヘルプの領域を一巡。課題は計 471 件)。残りは本文の「在庫候補」印を台帳へ移す整理だけ
