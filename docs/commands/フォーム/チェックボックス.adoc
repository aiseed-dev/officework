= チェックボックス

フォームにあります。

*状態: 実装済み* — いま使えます

== 何をするか

`☐` と `☑` を切り替える記入欄を置きます。

すでにチェックボックスの中にカーソルがあるときは、*その場で切り替えます*。
新しく置きたいときは、欄の外にカーソルを出してから押してください。

== ダイアログ

(ダイアログは出ません)

== プログラムから

[source,python]
----
mcp.doc_fill({名前: 値})
----

