= 区切り位置

データにあります。

*状態: 実装済み* — いま使えます

== 何をするか

1つのセルの字を、区切りで分けて隣の列に配ります。

== ダイアログ

区切り文字を打つ欄が1つ出ます。複数の文字を入れられます。

**欄はこれだけ**です。Excel は3段のウィザードで、固定長で切る形や、
列ごとの型の指定もありますが、ここにはありません。

== プログラムから

専用の呼び方はありません。こう書けば同じことができます。

[source,python]
----
s['B2'], s['C2'] = s['A2'].split(',')
----

