= フィル

ホームにあります。

*状態: 実装済み* — いま使えます

== 何をするか

隣のセルの規則を読んで、続きを埋めます。

== ダイアログ

(ダイアログは出ません)

選んだ範囲に、上のセルの値か連続した数を埋めます。

== プログラムから

専用の呼び方はありません。こう書けば同じことができます。

[source,python]
----
for i in range(10): s[f'A{i+2}'] = i + 1
----

