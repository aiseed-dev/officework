= フォルダーを開く

ファイルのページにあります。

*状態: 未実装* — *まだ使えません。* これから作ります。
いまは、起動のときにフォルダを選べます。

== 何をするか

仕事のフォルダを開き直します。

**フォルダが綴りです。** 中の文書・表・見た目のテンプレートが、まとめて1つの仕事になります。
開くと、左の一覧にフォルダの中身が出ます。

== ダイアログ

フォルダを選ぶ画面が出ます。ファイルは選びません。

開いた後、一覧から押したものが編集画面に出ます。
この道具で開けない種類(`.ipynb` `.py` など)を押したときは、
*その機械で結び付けてある道具*が起きます。

== プログラムから

(まだありません)

プログラムはフォルダを開く必要がありません。径路を直に書けます。

[source,python]
----
from pathlib import Path
for p in Path("仕事のフォルダ").glob("*.adoc"):
    ...
----
