= ページ番号

挿入にあります。

*状態: 実装済み* — いま使えます

== 何をするか

いまのページ番号を差し込みます。紙にしたときに実際の数字になります。

== ダイアログ

選ぶものはありません。押した所にページ番号の印が入ります。

ヘッダーやフッターを開いていないときは、フッターが自動で開きます。

**位置や書式の選択はありません。** Word は位置(上・下・余白)と配置と
書式を選べますが、ここでは*カーソルのある所*に入ります。

== プログラムから

専用の呼び方はありません。こう書けば同じことができます。

[source,python]
----
(ヘッダーの `#`)
----

